# 커스텀 버튼 (구 "빠른 토글" — 뷰템플릿/필터/작업세트 원클릭 on/off + 뷰 저장/되돌리기)

**이름 변경 (2026-07-28)**: 사용자 요청으로 이 기능의 사용자 노출 명칭을 "빠른 토글"에서 "커스텀 버튼"으로
바꿨다 — 리본 패널명(`App.QuickTogglePanelName`), 버튼 라벨/툴팁, 창 제목(`QuickToggleToolbar.xaml`/
`QuickToggleSettingsWindow.xaml`의 `Title`), `TaskDialog` 제목, 트랜잭션 이름, `IExternalEventHandler.
GetName()` 등 실제 화면에 보이는 문자열만 바꿨다. 클래스/파일 이름(`QuickToggle*`)과 코드 주석(과거
날짜의 개발 이력)은 그대로 두었다 — 코드 심볼 전체를 리네이밍하는 건 이번 요청 범위를 넘어서는 대규모
작업이고, 주석은 그 시점의 의사결정 기록이라 소급 변경하지 않는다(README/CLAUDE.md의 새 기술에서는
"커스텀 버튼(구 "빠른 토글")"으로 병기해 과거 문서와의 연결을 남겼다).

`QuickToggleSettings.cs`/`QuickToggleService.cs`/`QuickToggleIcons.cs`/`QuickToggleExternalEventHandler.cs`/`QuickToggleToolbar.xaml(.cs)`/`QuickToggleSettingsWindow.xaml(.cs)`를 건드리기 전에 이 문서를 읽을 것. 이 프로젝트에서 유일하게 모드리스 창·`ExternalEvent`·Win32 P/Invoke를 쓰는 영역이라 다른 창들과 아키텍처가 다르다.

**먼저 확인 (2026-09-02)**: "프리셋 버튼"과 "그래픽 화면표시 검색 버튼"은 **삭제됐다**. 아래 본문에 나오는 그
두 기능 관련 서술(프리셋 탭 구조, 카테고리 V/G 재정의 편집창, 검색 패널 등)은 전부 **과거 이력**이며 지금
코드에는 없다 — 문서 맨 아래 "프리셋·그래픽 화면표시 검색 버튼 삭제 + 링크 끄기/켜기 버튼 추가" 항목을 먼저
읽을 것(특히 열거 멤버 `Preset`/`GraphicsDisplaySearch`를 지우면 안 되는 이유).

2026-07-24 추가, 사용자 요청("현재 뷰의 뷰템플릿/필터/작업세트를 딸깍 한 번으로 on/off"). **Revit API는 신속접근 도구모음(QAT)에 버튼을 프로그램적으로 추가하는 기능을 지원하지 않는다** — 사용자에게 이 제약을 먼저 확인했고, 대신 Revit 메인 창 상단에 고정되는 자체 커스텀 플로팅 창(`QuickToggleToolbar`)으로 대체 구현했다. 이 기능은 이 프로젝트에서 처음으로 **모드리스(비모달)로 세션 내내 떠 있는 창**을 도입했고, 그로 인해 이 프로젝트 최초로 `ExternalEvent`/`IExternalEventHandler`, `UIControlledApplication.ViewActivated`/`Idling`/`ControlledApplication.DocumentClosing` 이벤트 구독, Win32 P/Invoke(`GetWindowRect`), 프로젝트 파일 경로별 설정 저장이 필요해졌다 — 기존 창은 전부 커맨드 `Execute` 안에서 여는 모달 `ShowDialog()`라 이런 게 하나도 필요 없었다.

- **`QuickToggleSettings.cs`**: `QuickToggleButtonConfig{ Id(Guid), Name, Category(ViewTemplate/Filter/Workset/Preset), ViewTemplateId(단일), FilterIds/WorksetIds(리스트), IconShape(nullable), OnColorHex(nullable) }`의 목록. `IconShape`/`OnColorHex`는 사용자가 버튼마다 아이콘 모양·on 색을 직접 고를 수 있게 해달라는 요청(2026-07-27)으로 추가 — 둘 다 null이면 예전 그대로 카테고리 기본 아이콘/공용 on 색을 쓰므로 기존 저장 파일과도 호환된다. 카테고리당 버튼 1개가 아니라 "뷰템플릿버튼1", "뷰템플릿버튼2"처럼 이름 붙인 버튼을 몇 개든 추가할 수 있다(사용자 요청). `%APPDATA%\WallSplitter\quick-toggle\<sha256(프로젝트경로)>.json`에 **프로젝트 파일 경로별로** 저장 — 기존 `NamingSettings`(PC 전체 전역)와 달리 프로젝트마다 독립적이고, 이 PC를 벗어나면 따라가지 않는다(명시적 요청사항). `ElementIdCompat.ToInt()` 확장 메서드로 `ElementId`를 int로 저장하는데, `IntegerValue`는 2023 API에만 있고 2024+에서는 **완전히 제거**돼 컴파일 자체가 깨진다(2026 빌드로 실측 확인 — obsolete 경고만 있을 거라는 최초 가정은 틀렸음) — `#if REVIT2023`로 `IntegerValue`/`(int)Value`를 분기한다.
- **`QuickToggleIcons.cs`** (신규 파일, 2026-07-27 추가): 빠른 토글 버튼이 그리는 벡터 아이콘 모양(`QuickToggleIconShape`: `Layers`/`Funnel`/`Lines`/`Star`/`Flag`/`Dot`/`Eye`/`Flower`/`Heart`/`Bolt` — 뒤 4개는 "아이콘 갯수를 늘려달라(눈/꽃 모양 등)"는 요청으로 같은 날 추가)과 그리기 로직(`Create`)을 `QuickToggleToolbar`(실제 툴바 렌더링)와 `QuickToggleSettingsWindow`(설정 창의 아이콘 선택 미리보기)가 공유한다 — 이 프로젝트는 보통 "작은 UI 패턴은 각 창에 복제"하는 관례지만, 여기는 그리기 로직이 어긋나면 미리보기와 실제 툴바 모습이 달라 보이는 문제가 생기므로 예외적으로 공유 클래스로 뺐다. `DefaultFor(category)`가 `IconShape`를 사용자가 직접 고르지 않았을 때(null)의 카테고리별 기본값(뷰템플릿→`Layers`, 필터→`Funnel`, 작업세트→`Lines`)을 정한다 — 이 기능이 추가되기 전의 동작 그대로. `Eye`/`Heart`는 `GeometryGroup`(`EvenOdd`/`Nonzero` 채우기 규칙)으로 여러 도형을 합치거나 구멍을 뚫어 만든다(`Eye`= 베지어 곡선 아몬드형에서 원을 `EvenOdd`로 오려냄, `Heart`=원 2개+삼각형을 `Nonzero`로 합침) — `Rectangle`/`Polygon`/`Line`/`Ellipse`만 다루던 `SetBrush`도 `Path` 케이스를 추가해서 맞췄다. `Flower`는 타원 6개를 `RenderTransformOrigin=(0.5,1.0)`(자기 자신의 아래쪽 끝) 기준으로 60°씩 회전시켜 중심에서 방사형으로 배치한 것 — 스크래치패드에 `RenderTargetBitmap`으로 각 아이콘을 실제 렌더링해 미리보기 PNG를 뽑아본 뒤(모양이 알아볼 수 있게 나오는지 직접 확인 후) 반영했다. **`ContrastingForeground(Color)`** (같은 날 추가): 상대 휘도 근사식(`0.299R+0.587G+0.114B`, 임계값 150)으로 밝은 배경엔 `Theme.TextPrimary`(어두운 글자), 어두운 배경엔 `Theme.OnAccent`(밝은 글자, `Theme.xaml`의 `OnAccentBrush`와 맞춘 신규 `Theme.cs` 필드)를 반환 — "버튼 색상을 지정하면 배경 자체가 바뀌고 아이콘/텍스트는 그 배경에 대비되어 자동으로 잘 보이는 색이 되어야 한다"는 요청으로 추가했다. `ColorPalette`(8종 프리셋, `QuickToggleSettingsWindow.cs`)는 전부 채도 있는 중간~어두운 톤이라 실제로는 전부 `OnAccent`(밝은 글자) 분기만 타지만, 함수 자체는 어떤 색이 와도 일반적으로 동작한다.
- **`QuickToggleService.cs`**: 뷰템플릿 on/off는 `View.ViewTemplateId`를 지정 id 또는 `ElementId.InvalidElementId`로 설정. 필터 off는 **"표시(Visibility)" 체크박스만 끄고 필터 자체는 뷰에 남긴다**(`SetFilterVisibility(id,false)`만 호출, "사용/재정의" 체크박스는 안 건드림 — 명시적 요청사항). 작업세트 on/off는 `SetWorksetVisibility(id, Visible/Hidden)`. 상태 판정(`DetermineState`)은 뷰가 그 카테고리를 지원하지 않으면(예: 스케줄의 필터) 예외를 잡아 `Disabled`로 처리 — 뷰 타입별 지원 여부를 직접 나열하지 않는다.
  - **코드 결함, 수정 (2026-07-27, 사용자가 "버튼을 눌러도 잘 작동하지 않는 것 같다"고 보고해서 재점검 중 발견)**: `Toggle()`의 뷰템플릿 케이스만 Filter/Workset과 달리 `try/catch`가 없었다 — 대상 뷰템플릿이 현재 뷰 종류/카테고리와 호환되지 않으면 `view.ViewTemplateId` 설정이 예외를 던지는데, `QuickToggleExternalEventHandler.Execute`는 `ExternalEvent` 콜백이라 예외가 나도 Revit이 사용자에게 아무 오류도 보여주지 않고, 트랜잭션은 자동 롤백되어 "버튼을 눌러도 반응 없음"으로만 보였다. `Toggle()`이 이제 `bool`(실제 반영 여부)을 반환하도록 고치고, 뷰템플릿 케이스도 다른 두 케이스처럼 `try/catch`로 감싸 실패를 알린다. `Execute()`는 `Toggle()`이 `false`를 반환하거나 `tx.Commit()`이 `Committed`가 아니면 `TaskDialog`로 실패를 직접 알리도록 고쳤다(이전엔 둘 다 확인하지 않아 실패가 완전히 조용했다).
- **`QuickToggleExternalEventHandler`**: 툴바 버튼 클릭 → `PendingButtonId`/`PendingTurnOn` 세팅 후 `ExternalEvent.Raise()` → `Execute(UIApplication)`에서 트랜잭션 열고 반영 후 `QuickToggleToolbar.Instance.RefreshState()` 직접 호출(같은 Revit UI 스레드라 Dispatcher 불필요).
- **`QuickToggleToolbar`**: `WindowStyle=None`/`ShowInTaskbar=false`, `WindowInteropHelper.Owner=MainWindowHandle`(기존 `SettingsCommand`와 같은 오너 지정)로 Revit 창 위에 계속 뜬다. `App.OnStartup` 시점엔 완전한 `UIApplication`을 만들 방법이 없어(`UIControlledApplication.ControlledApplication`은 별개 타입인 `ControlledApplication`이라 `UIApplication` 생성자에 못 넘김 — 실제로 빌드해서 확인) `ViewActivated`/`Idling`의 `sender`를 `UIApplication`으로 캐스트해 **첫 이벤트에서 지연 생성**한다. 버튼 목록은 원래 `ScrollViewer`(가로 스크롤)에 담아 한 줄에 다 안 들어가면 옆으로 스크롤하게 했었으나, 2026-07-27 "스크롤이 너무 짧아 잡기 힘들고 반대로 되는 느낌" 피드백으로 **`ScrollViewer`를 완전히 제거**하고 버튼이 많아지면 창 자체(`ResizeToContent()`)가 옆으로 계속 늘어나도록 바꿨다(최대 너비 제한도 함께 제거) — Revit 메인 창보다 넓어질 수 있다는 트레이드오프는 있지만, 스크롤보다 이쪽을 원한다는 명시적 요청이었다. `Idling`은 매우 자주 발생하므로 그때마다 설정 파일을 다시 읽지 않고 문서가 바뀐 경우에만(`EnsureSettingsLoaded`) 디스크에서 재로드 — 같은 문서에서의 반복 갱신은 캐시된 설정 + 최신 뷰 상태 조회만으로 처리해 유휴 틱마다 파일 I/O가 발생하지 않게 했다.
  - **원래 설계 반전 (2026-07-27, 사용자 피드백) — 고정 위치 → 드래그 가능한 패널**: 최초 Q&A에서 "Revit 창 상단에 고정, 드래그 불가"로 확정했었지만(리본 높이를 실측 오프셋 하나로 맞추는 방식, 기본값을 여러 번 조정해도 여전히 리본 버튼을 가리는 문제가 있었음), 사용자가 "직접 움직여서 위치를 지정할 수 있는 도킹패널이었으면 좋겠다"고 명시적으로 요청해 드래그 가능하게 바꿨다. `DragMove()`가 반환되면(마우스를 뗀 시점) 그 위치를 Revit 메인 창 기준 오프셋(`QuickToggleSettings.ToolbarOffsetXDip`/`ToolbarOffsetYDip`, 프로젝트별 저장)으로 환산해 즉시 저장 — "Revit 창이 움직이면 따라다녀야 한다"는 원래 요구사항은 유지하면서, 그 오프셋 자체를 사용자가 드래그로 직접 정하게 했다. `QuickToggleSettingsWindow`엔 화면 밖으로 드래그된 경우를 위한 "위치 초기화" 버튼만 남기고, 예전에 추가했던 숫자 입력란은 제거했다(드래그가 더 직접적인 대체 수단이라 중복).
  - **CONFIRMED LIVE BUG (2026-07-27), 두 번째 수정 — "드래그가 안 된다" + "버튼이 잘 안 잡힌다" 동시 보고**: 최초 구현은 `RootBorder`(버튼이 없는 빈 영역 전체)에 `MouseLeftButtonDown` → `DragMove()`를 연결했었다. 그런데 창이 `SizeToContent`로 버튼 내용에 딱 맞게 줄어들다 보니 실제로 클릭 가능한 "빈 배경"이 버튼 사이 2~4px짜리 좁은 틈밖에 안 남아, 사용자가 "드래그가 안 된다"(그 좁은 틈을 못 잡아서)와 "버튼 클릭이 잘 안 잡힌다"(버튼 사이 좁은 틈에서 클릭이 씹혀서)를 동시에 보고했다 — 사실상 같은 원인(드래그용 여백과 버튼 클릭 영역이 서로 너무 가깝고 좁음)이었다. 고정: (1) 왼쪽에 항상 존재하는 전용 드래그 그립(`DragGrip_MouseLeftButtonDown`, 점 3개 아이콘, 고정 16px 너비 - `QuickToggleToolbar.xaml`의 `Grid` 첫 번째 열)을 따로 둬서 드래그는 거기서만 하고, 버튼 영역(`ButtonsPanel`)에는 더 이상 드래그 핸들러를 두지 않는다 - 버튼 개수·창 너비와 무관하게 "여기를 잡으면 옮길 수 있다"는 위치가 항상 고정으로 보장된다(`ResizeToContent()`도 이 그립의 고정 너비 `GripWidthDip`을 버튼 내용 너비에 더해서 창 전체 너비를 계산하도록 맞춤). (2) 각 버튼의 시각적 여백을 Button 바깥 `Margin`이 아니라 안쪽 `content`(아이콘+라벨을 담은 StackPanel)의 `Margin`으로 옮겼다 - 바깥 `Margin`은 히트테스트 영역이 아니라서 버튼 사이에 클릭이 씹히는 좁은 사각지대가 있었는데, 이제 `Button` 자체는 옆 버튼과 완전히 맞닿아 그 사각지대가 없다.
  - **CONFIRMED LIVE BUG (2026-07-27), 수정 — 클릭이 잘 안 잡히는 문제 (1차)**: 사용자가 "마우스를 가져다 대면 클릭이 잘 안 잡힌다"고 실측 보고 — 원인은 `RefreshState()`가 `ViewActivated`뿐 아니라 초당 여러 번 발생하는 `Idling`에서도 호출되는데, 그때마다 `RebuildButtons()`가 `ButtonsPanel.Children.Clear()` 후 모든 `Button`을 새 인스턴스로 다시 만들었다는 것이었다. 마우스 클릭은 `MouseDown`(누름)과 `MouseUp`(뗌) 사이에 시간차가 있는데, 그 사이에 `Idling`이 한 번이라도 끼어들면 눌렀던 바로 그 `Button` 인스턴스가 이미 지워지고 새 인스턴스로 교체되어 있어 `Click` 이벤트가 아예 발생하지 않았다 — 간헐적이라 "클릭이 잘 안 잡힌다"처럼 느껴졌다. 고정: `_renderedButtons`(마지막으로 구조를 지은 `_cachedSettings.Buttons` 리스트 참조)를 기준으로, 실제로 목록이 바뀐 경우(설정 저장/문서 전환 — `ForceReloadSettings`가 새 `QuickToggleSettings` 인스턴스를 만들 때만 리스트 참조가 바뀜)에만 `RebuildButtons()`로 구조를 다시 짓고, 그 외의 매 `Idling` 틱에는 `UpdateButtonStates()`가 이미 있는 같은 `Button`/아이콘/라벨 인스턴스의 색·툴팁·활성 여부만 갱신한다(`QuickToggleIcons.SetBrush` 헬퍼로 도형 종류에 맞게 색만 갱신). 인스턴스 자체가 더 이상 파괴·재생성되지 않으므로 클릭 도중 대상이 바뀌는 경합이 사라진다.
  - **CONFIRMED LIVE BUG (2026-07-27), 수정 — 클릭이 잘 안 잡히는 문제 (2차)**: 1차 수정 이후에도 "마우스를 움직이면서 눌러야 겨우 클릭되고, 멈추면 클릭이 안 된다"는 훨씬 더 구체적인 재보고를 받았다. 원인은 `RepositionToMainWindow()`가 새로 계산한 위치가 이전과 같든 다르든 매번 `Left`/`Top`을 대입했다는 것 — 이 메서드도 `RefreshState()`를 통해 `Idling`마다 호출되는데, `Idling`은 정확히 "처리할 메시지가 없을 때"(=마우스가 멈춰 있을 때) 가장 자주, 거의 끊임없이 발생한다. WPF의 `Window.Left`/`Top` setter는 값이 같아도 내부적으로 Win32 `SetWindowPos`를 호출하는데, 이게 사용자가 버튼을 누르고 있는 도중 반복되면 `ButtonBase`의 마우스 캡처가 끊겨 `MouseUp`이 완성되지 못했다 — 마우스를 움직이는 동안은 `WM_MOUSEMOVE` 메시지가 큐를 채워 `Idling`이 상대적으로 덜 끼어드니 우연히 클릭이 되곤 했던 것. 고정: 계산된 새 위치가 현재 위치와 실제로 0.5px 이상 차이 날 때만 `Left`/`Top`을 대입한다(`PositionEpsilonDip`).
  - **CONFIRMED LIVE BUG (2026-07-27), 수정 — 클릭이 잘 안 잡히는 문제 (3차, 진짜 근본 원인)**: 2차 수정 이후에도 "아직도 클릭이 잘 안 된다"는 재보고를 받았다. 같은 패턴이 한 군데 더 있었다 - `UpdateButtonStates`가 상태가 실제로 바뀌었든 아니든 매 `Idling` 틱마다 `button.IsEnabled`/`button.ToolTip`/아이콘 색을 무조건 다시 대입하고 있었다. `IsEnabled` 재대입은 값이 그대로여도 WPF의 강제(coerce) 파이프라인을 다시 태우고, `ToolTip` 재대입은 매번 `ToolTipFor`가 만드는 새 문자열 인스턴스라 `ToolTipService`가 툴팁을 다시 여닫으려 든다 - 사용자가 버튼을 누르고 있는 도중이면 둘 다 마우스 캡처를 끊을 수 있다. 고정: 버튼별 마지막 상태를 `_lastStates`(`Dictionary<string /*cfg.Id*/, QuickToggleButtonState>`)에 기억해두고, `RebuildButtons`에서 초기값을 채운 뒤 `UpdateButtonStates`는 실제로 상태가 바뀐 버튼만 갱신한다. **곁들여 방어적으로 보강한 것**: `EnsureSettingsLoaded`가 "문서가 바뀌었는지"를 `ReferenceEquals`만으로 판단했는데, Revit API가 같은 열린 문서에 대해 매 호출마다 정확히 같은 .NET 래퍼 인스턴스를 돌려준다는 보장을 확인하지 못했다 - 만약 그 가정이 틀리면 매 틱마다 "문서가 바뀐 것"으로 오판해 `ForceReloadSettings`가 매번 새 `Buttons` 리스트를 만들고, 그러면 `RebuildButtons` 판정도 매 틱마다 참이 되어 원래 버그(1차)가 다른 경로로 되살아난다 - `DocumentsMatch`(`ReferenceEquals` 또는 `Equals` 또는 `PathName` 일치)로 이중 방어했다(직접 재현/확인된 버그는 아니고, 같은 클래스의 문제가 반복된 걸 보고 선제적으로 보강한 것). **세 차례 수정 모두 같은 교훈**: `Idling` 콜백 안에서는 실제로 뭔가 달라졌는지 먼저 확인하고, 달라졌을 때만 손댈 것 — 매 틱 실행되는 코드에서 "같은 값 재대입"조차 공짜가 아니다.
  - **드래그 그립 확대 (2026-07-27, "잡을 수 있는 픽셀이 너무 작아서 잡기 힘들다")**: `GripWidthDip`(및 XAML `Border Width`) 16→28, 점 3개도 3px→4px로 비례해서 키웠다. 두 값을 반드시 같이 맞춰야 한다는 기존 주석의 원칙 그대로.
  - **CONFIRMED LIVE BUG (2026-07-27), 그립 확대 이후에도 "점 세 개에 정확히 마우스를 가져다 대야만 움직여진다"**: 그립 `Border`에 `Background`를 지정하지 않아서(기본값 `null`) 28px 너비 전체가 아니라 실제로 눈에 보이는 점 3개(`Rectangle`) 자신의 아주 작은 영역만 히트테스트됐다 - 다크 테마 전환 때 겪었던 "버튼 사이 빈 공간이 클릭 안 됨"과 같은 부류의 문제가 이번엔 그립 자체에서 재발한 것. 고정: `Border`에 `Background="Transparent"`를 명시(투명해도 히트테스트는 됨)하고, 안쪽 점 3개 `StackPanel`엔 `IsHitTestVisible="False"`를 줘서 클릭이 항상 `Border` 자신에서 잡히도록 했다 - 이제 28px 너비, 행 높이 전체가 어디를 눌러도 드래그된다.
  - **아이콘 확대 (2026-07-27, "아이콘도 좀 크게 해달라")**: `QuickToggleIcons.Create`가 내부적으로 쓰는 20x16 좌표계는 그대로 두고(10종 도형의 좌표를 전부 다시 계산할 필요 없이), `RebuildButtons`가 아이콘 `Canvas`를 `Viewbox`(28x22)로 감싸서 렌더링 크기만 키웠다 - 재색칠(`QuickToggleIcons.SetBrush`)은 `_rows`에 저장해둔 원본 `Canvas` 참조를 그대로 쓰므로 이 래핑과 무관하게 계속 동작한다.
  - **버튼 색상 지정이 아이콘 색만이 아니라 배경 전체를 채우도록 확장 (2026-07-27, "버튼 배경 자체가 다 변하고, 아이콘/텍스트는 대비되는 색으로 자동 변경")**: 예전엔 `cfg.OnColorHex`가 아이콘 `Fill`에만 쓰이고 버튼 배경은 항상 기본 스타일(Industry 테마의 투명 "선 그림") 그대로였다. `BrushFor(state, cfg)`를 `VisualsFor(state, cfg)`(반환값 `(Background, BorderBrush, Foreground)` 3-튜플)로 바꿔, On 상태일 때 `Button.Background`/`BorderBrush`를 실제로 그 색(사용자 지정 또는 기본 `Theme.ToggleOn`)으로 채우고 `Foreground`는 `QuickToggleIcons.ContrastingForeground`가 배경 밝기에 맞춰 자동으로 고른다. Off/Disabled는 그대로 투명 배경 + 하이라인 테두리 유지(Industry 테마의 "주 버튼만 채워진 입체" 시각 언어와 일치). `RebuildButtons`/`UpdateButtonStates` 양쪽 다 이 3-튜플로 `Button.Background`/`BorderBrush`/아이콘/라벨 색을 한 번에 갱신하도록 맞췄다. **알려진 사소한 제약**: `Button`의 `IsMouseOver`/`IsPressed` 하이라이트는 `BaseButtonStyle`의 `ControlTemplate.Triggers`가 내부 `Chrome`(테두리) 엘리먼트를 직접 겨냥해 설정하므로, 색이 채워진 On 버튼을 마우스로 hover하면 순간적으로 일반 회색조 hover 틴트가 겹쳐 보였다가 마우스를 떼면 다시 원래 색으로 돌아온다 - 버튼마다 커스텀 hover 트리거를 만들어야 완전히 없앨 수 있는데, 이번 요청 범위를 넘어서는 과한 작업이라 하지 않았다.
- **`QuickToggleSettingsWindow`**: 왼쪽에 등록된 버튼 목록(`SettingsWindow`의 토큰 목록과 동일한 위/아래 이동·삭제 패턴), "뷰템플릿/필터/작업세트 버튼 추가" 3개 버튼, 오른쪽에 선택된 버튼의 이름+대상 편집 영역(뷰템플릿=단일선택 라디오, 필터/작업세트=체크박스 다중선택), 하단에 "위치 초기화" 버튼(`ResetPositionButton`, 화면 밖으로 드래그된 경우의 안전장치 - 위 QuickToggleToolbar 항목 참고). 워크셰어링 안 된 문서는 작업세트 버튼 추가 자체를 비활성화. `Autodesk.Revit.DB`를 이 파일에서 함께 쓰다 보니 **`Grid`가 `System.Windows.Controls.Grid`/`Autodesk.Revit.DB.Grid`(데이텀 그리드선) 사이에서 모호해지는 새로운 충돌 사례**를 실제 빌드로 발견 — `Visibility`/`Control`/`Color`/`Binding`에 이어 이 프로젝트에서 처음 마주친 `Grid` 충돌이라 `WpfGrid` 별칭으로 구분했다(WPF 쪽만 별칭 부여, DB 쪽이 훨씬 자주 쓰여서). `QuickToggleToolbar.xaml.cs`는 반대로 `Line`/`Point`(둘 다 `Autodesk.Revit.DB`에도 있음)가 충돌해 `Autodesk.Revit.DB`를 통째로 안 쓰고 `Document`/`View`만 별칭(`RevitDocument`/`RevitView`)으로 가져오는 방식을 택했다 — 어느 쪽을 별칭 처리할지는 그 파일에서 어느 네임스페이스 쪽 사용 빈도가 더 높은지로 정함.
  - **레이아웃 조정 (2026-07-27, 사용자 요청 — "이름/아이콘/색상 부분이 너무 커서 아래 대상 선택 부분이 작아 보인다")**: `BuildEditPanel`/`BuildIconAndColorPicker`의 위아래 여백(이름 칸, "아이콘"/"켜짐 색상" 레이블, 두 `WrapPanel`의 아래쪽 마진)을 전부 최소치로 줄이고, 아이콘/색상 스와치 크기도 32px/26px → 24px/20px로 줄였다 — 아이콘이 6종에서 10종으로 늘면서(위 `QuickToggleIcons.cs` 항목 참고) 안 그래도 넉넉했던 위쪽 영역이 더 커질 상황이었던 것도 같이 상쇄된다.
  - **CONFIRMED LIVE BUG (2026-07-27), 수정 — "스크롤을 내리면 이름 변경 칸이 같이 사라진다"**: 오른쪽 편집 영역 전체(이름 입력칸 + 카테고리 안내 + 실제 대상 목록)가 하나의 `ScrollViewer` 안에 있었다 — 뷰템플릿이 많아 목록이 길어지면 스크롤을 내렸을 때 위쪽 이름 칸까지 같이 화면 밖으로 밀려났다. 고정: 오른쪽 `Border` 안을 `Grid`(Auto 행 + `*` 행)로 나눠 이름 입력칸/카테고리 안내는 `EditHeaderHost`(스크롤 없음, 항상 고정)에, 실제 대상 목록만 `EditPanelHost`(그 안의 `ScrollViewer`, 스크롤됨)에 넣도록 `BuildEditPanel`/`ShowEmptyEditPanel`을 나눴다.
  - **CONFIRMED LIVE BUG (2026-07-27), 수정 (1차) — 위/아래/삭제 버튼이 "투명하게 보인다"**: 다크 테마 때는 기본 `Button` 스타일의 배경이 채워진 색이라 이 22px짜리 작은 아이콘 버튼들도 뚜렷하게 보였는데, Industry 라이트 테마로 바뀌며 기본 버튼 배경이 투명(테두리만 있는 "선 그림", `readme`의 `.btn-secondary` 규칙)이 되어 존재감이 옅어졌다. `RefreshButtonList`의 위/아래/삭제 버튼에 `Background=Theme.Surface`를 명시적으로 채우고, `CreateTriangle`/`CreateXMark`가 그리는 도형도 10x8→12x10(삭제 X는 10x10→12x12, 선 두께 1.5→2)으로 키우고, `Width`/`Height`를 22→24로 키웠었다.
  - **CONFIRMED LIVE BUG (2026-07-27), 수정 (2차, 진짜 원인) — "여전히 안 보이고 이상하다"**: 1차 수정 이후에도 재보고를 받아 실제로 렌더링해서 확인해보니(스크래치패드에 `RenderTargetBitmap`으로 실제 `Theme.xaml` 적용된 `Button`을 그려봄), 도형 자체가 거의 다 잘려나가 있었다. 원인: `Width`/`Height`만 24로 키우고 `Padding`은 그대로 뒀는데, `BaseButtonStyle`의 기본 `Padding`이 `"10,5"`라 24x24 버튼의 실제 콘텐츠 영역이 `24-10*2=4` x `24-5*2=14`로 쪼그라들어 12x10짜리 삼각형/12x12 X가 거의 다 잘렸던 것 — 색이나 대비 문제가 전혀 아니라 순수 레이아웃(Padding) 문제였다. 고정: 세 버튼 모두 `Padding=new Thickness(2)`를 명시해서 도형이 실제로 버튼 안에 다 들어오게 했다. 이 문제는 "값을 키우면 당연히 더 잘 보이겠지"라고 추측만 하고 실측하지 않아서 놓쳤던 것 - 이후 렌더링해서 직접 눈으로 확인하는 방식으로 검증했다.
- **리본**: "빠른 토글" 패널에 "빠른 토글 설정"(`QuickToggleSettingsCommand`) + "표시/숨김"(`QuickToggleVisibilityToggleCommand`, `App.AddSettingsStack`과 같은 스택 패턴) 두 버튼. "표시/숨김"은 설정이 프로젝트별이라 라벨("켜짐"/"꺼짐")이 활성 문서가 바뀔 때마다 `ViewActivated`에서 다시 맞춰진다. 아이콘 `icon_quicktoggle{16,32}.png`(번개 모양, 기존 flat line-art 스타일과 통일된 강조색 `#5B8DEF`)는 스크래치패드 `System.Drawing` 생성기로 새로 그림.
- **"뷰 저장"/"되돌리기" 고정 버튼** (2026-07-27 추가, 사용자 요청 — "현재 활성화되어 있는 뷰의 현재상태의 뷰를 저장해두고, 나중에 뷰가 여러번 바뀌더라도 되돌리기 버튼을 누르면 저장했던 시점의 뷰 상태로 되돌아 갈 수 있었으면 좋겠다. 모델의 변경사항 등은 변경된 상태로 그대로 유지"): 등록된 버튼 목록과 무관하게 툴바 우측 끝에 항상 붙는 두 버튼 (`QuickToggleToolbar.xaml`의 3번째 `Grid` 열, `RightControlsPanel`). 뷰템플릿/필터 표시/작업세트 표시라는 "이 뷰에 무엇이 보이는지"만 찍어두고, 모델 자체(형상·파라미터 등)는 전혀 건드리지도 기록하지도 않는다 — 빠른 토글이 원래 다루는 세 가지 축을 그대로 재사용하는 설계.
  - **`ViewStateSnapshot`** (`QuickToggleExternalEventHandler.cs`): `ViewId`/`ViewTemplateId`(int?)/`FilterVisibility`(Dictionary<int,bool>)/`WorksetVisibility`(Dictionary<int,bool>, true=Visible)만 담는 순수 메모리 값 — 디스크에 저장하지 않는다(다른 설정들과 달리 프로젝트 종료 후 남길 이유가 없고, `ElementId`도 그 문서를 벗어나면 무의미하므로). `QuickToggleToolbar._savedViewState` 필드 하나에 최근 저장 1건만 보관 (여러 개 저장/이력 관리 기능은 요청 범위 밖).
    - **범위 확장** (2026-07-28, 사용자 요청 — "모델,주석,해석모델,가져온카테고리,필터,작업세트, 뷰템플릿, 뷰자르기 및 범위 등등 모든 요소들을 기억하고 있는 상태"): `CategoryHidden`(Dictionary<int,bool>, true=Hidden)과 `CropBoxActive`/`CropBoxVisible`/`CropBox`(`BoundingBoxXYZ?`)/`PlanViewRange`(`PlanViewRange?`, 평면 뷰가 아니면 null) 필드를 추가했다. Revit의 가시성/그래픽 재정의(V/G) 대화상자는 모델/주석/해석 모델/가져온 카테고리를 별도 탭으로 보여주지만, 실제로는 전부 `doc.Settings.Categories`라는 하나의 카테고리 트리에서 나온 `Category` 객체이고 `View.GetCategoryHidden`/`SetCategoryHidden`도 카테고리 종류를 구분하지 않으므로, 탭별로 따로 나누지 않고 `QuickToggleService.AllCategories`(최상위 카테고리 + 모든 하위 `SubCategories`를 재귀 순회)로 트리 전체를 한 번에 다룬다 — 이 방식으로 가져온 CAD/이미지의 레이어별 카테고리도 자연히 포함된다(단, Revit이 실제로 이들을 이 트리 밖의 다른 메커니즘으로 노출하는 경우가 있는지는 **실측 미검증** — 안 된다는 보고가 있으면 이 지점부터 확인할 것). "뷰 자르기 및 범위"는 크롭(활성 여부/표시 여부/크롭 상자 자체)과, 평면 뷰 전용의 뷰 범위(`ViewPlan.GetViewRange()`/`SetViewRange`, 상단/절단/하단/뷰 깊이 4개 평면의 레벨+오프셋을 한 번에 담는 Revit API 객체)로 캡처한다. `BoundingBoxXYZ`/`PlanViewRange` 모두 이 스냅샷이 디스크로 나가지 않고 메모리에만 머무르므로(같은 문서 내에서만 재사용됨), 원시 좌표/double로 풀어내지 않고 Revit API 객체를 그대로 들고 있다 — `ElementId`를 그대로 저장하는 기존 필드들과 같은 전제.
    - `QuickToggleService.CaptureViewState(View)`/`RestoreViewState(View, ViewStateSnapshot)`로 캡처·복원 로직 전체를 옮겨서 `QuickToggleToolbar.xaml.cs`(UI)와 `QuickToggleExternalEventHandler.cs`(트랜잭션 실행)는 얇게 유지했다 — `Toggle`/`DetermineState`와 같은 "Revit API를 직접 만지는 순수 로직, 트랜잭션은 호출자가 연다"는 이 클래스의 기존 계약을 그대로 따른다. 카테고리 수백 개를 순회하는 비용은 매 `Idling` 틱이 아니라 "뷰 저장"/"되돌리기" 버튼을 실제로 눌렀을 때만 발생하므로(등록형 버튼 목록과 달리 이 스냅샷은 유휴 틱마다 갱신되지 않음), 이 파일의 다른 부분들이 그렇게 조심하는 "매 Idling 틱마다 손대지 말 것" 규칙과는 무관하다.
  - **"뷰 저장"** (`SaveViewButton_Click`): 순수 읽기라 트랜잭션 없이 즉시 처리 — `QuickToggleService.CaptureViewState`를 호출해 스냅샷을 채운다. 저장 직후 되돌리기 버튼을 활성화하고 아이콘 색을 `Theme.TextSecondary`(활성)로 바꾼다 — 저장 전엔 `Theme.ToggleDisabled`(회색)로 비활성 표시.
    - **저장 상태 시각 표시** (2026-07-28 추가, 사용자 요청 — "뷰가 저장이 되고 있는지 전혀 알 수가 없어"): 이전엔 되돌리기 버튼만 상태를 반영하고 "뷰 저장" 버튼 자신은 클릭해도 겉모습이 전혀 안 바뀌어서, 저장이 실제로 됐는지 알 방법이 없었다. `UpdateSaveButtonVisual()`이 스냅샷 유무에 따라 다른 빠른 토글 버튼들의 On 상태와 같은 시각 언어(배경 전체를 `Theme.ToggleOn`으로 채우고 아이콘은 `QuickToggleIcons.ContrastingForeground`로 대비색 자동 선택 / 없으면 투명 배경 + `Theme.TextSecondary`)로 북마크 버튼을 표시한다. 생성자(초기 상태)와 `SaveViewButton_Click` 끝에서만 호출하면 되고, 이 버튼은 `RebuildButtons`/`UpdateButtonStates`가 매 `Idling` 틱마다 갱신하는 등록형 버튼 목록(`_rows`)에 속하지 않는 XAML 고정 버튼이라 그 사이 다른 코드가 덮어쓸 일이 없다.
  - **"되돌리기"** (`RevertButton_Click` → `ExternalEvent` → `QuickToggleExternalEventHandler.ExecuteRevert` → `QuickToggleService.RestoreViewState`): 다른 빠른 토글 버튼들과 똑같이 `ExternalEvent.Raise()` 경로를 그대로 재사용한다(같은 이유 — 모드리스 창의 클릭 시점엔 유효한 API 컨텍스트가 없음). 저장했던 뷰가 지금 활성 뷰와 다르면 `uidoc.ActiveView`를 그 뷰로 먼저 전환한 뒤(여러 번 다른 뷰를 거쳐도 저장했던 그 뷰로 돌아간다는 요청사항), 트랜잭션 안에서 뷰템플릿/필터 표시/작업세트 표시/카테고리 표시/크롭·범위를 스냅샷 값으로 재적용한다. 저장 시점 이후 필터/카테고리/워크셋/레벨이 삭제되었거나 뷰템플릿이 해당 항목을 제어하는 경우는 개별적으로 건너뛰고 나머지는 계속 적용(`try/catch`로 무시, 이 파일의 다른 메서드들과 같은 방어 스타일) — 표시 상태만 되돌릴 뿐 필터/카테고리 목록 구성 자체를 복원하지는 않는다.
  - **아이콘**: `QuickToggleIcons.CreateBookmarkIcon`(책갈피 리본 - 사각형+V자 노치)/`CreateUndoIcon`(반시계 방향 호+화살촉) — 스크래치패드 `RenderTargetBitmap` 렌더링으로 실제 형태 확인 후 반영. 이 두 아이콘은 `QuickToggleIconShape`(사용자가 고르는 버튼별 아이콘) 목록엔 없다 — 등록형 버튼이 아니라 항상 고정으로 붙는 전용 기능이라서.
  - `ResizeToContent()`가 `ButtonsPanel`뿐 아니라 `RightControlsPanel`도 함께 측정하도록 확장 — 안 그러면 등록된 버튼이 많을 때 창 너비가 이 두 고정 버튼을 자르고 그린다.
  - **뷰별 독립 저장 + 캡처 범위 대폭 확장 + 문서 간 격리** (2026-07-28, 두 번째 요청 — "뷰가 저장이 되고 있는지 전혀 알 수가 없어 ... 뷰를 저장한다는 것은 현재상태의 모델,주석,해석모델,가져온카테고리,필터,작업세트, 뷰템플릿, 뷰자르기 및 범위 등등 모든 요소들을 기억하고 있는 상태를 말하는 거야 ... 이 뷰저장 버튼은 각각 활성화되어있는 뷰마다 저장이 가능하고, 활성화된 뷰를 완전히 revit에서 x를 눌러 끄더라도 해당뷰에서는 저장되어있다"):
    - **뷰당 1개 → 뷰별 딕셔너리**: `QuickToggleToolbar._savedViewState`(필드 하나) 대신 `_savedViewStates`(`Dictionary<string, Dictionary<int, ViewStateSnapshot>>`, 바깥 키는 `DocKey(doc)`=`doc.PathName`(저장 안 된 새 문서는 `"__unsaved__"`로 뭉뚱그림), 안쪽 키는 `view.Id.ToInt()`)로 바꿨다. 바깥에 문서 키를 한 번 더 둔 이유: `ElementId`는 문서마다 독립적이라 서로 다른 문서를 동시에 열어둔 세션에서 같은 정수 ID가 전혀 다른 뷰를 가리킬 수 있기 때문 — 뷰 ID만으로 키를 잡으면 문서 A의 저장이 문서 B의 같은 ID 뷰에 잘못 "저장됨"으로 보일 수 있었다(구현 중 자체 발견, 실제 보고된 버그는 아님). "뷰를 닫아도(x) 유지"는 별도 구현이 필요 없었다 — 뷰를 닫는 것(탭 x)은 `View` 요소 자체를 지우지 않으므로 이 메모리 딕셔너리 항목이 자연히 그대로 남는다.
    - **"뷰 저장" 버튼 자체의 표시가 뷰마다 다름**: `RefreshSaveRevertButtons(view, force)`를 새로 만들어 `RefreshState()`(뷰 전환/`Idling` 양쪽에서 호출됨)에서도 호출한다. `_lastSaveButtonDocKey`/`_lastSaveButtonViewId`/`_lastSaveButtonHasSnapshot`로 마지막 확인값을 기억해두고, 셋 중 하나라도 실제로 바뀐 경우에만 WPF 속성을 재대입한다 — 이 파일에 이미 3차례 기록된 "매 `Idling` 틱마다 값이 같아도 재대입하면 클릭 캡처가 끊긴다" 버그 패턴을 새 코드에서 재도입하지 않기 위한 방어(위 `_lastStates` 항목과 같은 이유).
    - **캡처 범위 확장** (`ViewStateSnapshot`/`QuickToggleService.CaptureViewState`/`RestoreViewState`, 전부 세션 한정 메모리 값이라 `CropBox`/`PlanViewRange`와 같은 전제로 Revit API 객체를 가공 없이 그대로 담는다): `CategoryOverrides`(`Dictionary<int, OverrideGraphicSettings>`, `View.GetCategoryOverrides`/`SetCategoryOverrides` — "가시성/그래픽설정" = V/G 대화상자의 선/색/패턴/투명도/하프톤 재정의, 이전엔 "캡처하지 않음"이 알려진 제약이었으나 이번에 메웠다), `FilterOverrides`(필터별 `GetFilterOverrides`/`SetFilterOverrides`), `ColorFillSchemeId`(카테고리별 `GetColorFillSchemeId`/`SetColorFillSchemeId`, "색상표"), `DetailLevel`/`Autodesk.Revit.DB.DisplayStyle`(단순 속성, "상세수준"/"비주얼스타일" — 프로퍼티 이름이 타입 이름과 같아서(`View.DisplayStyle`) `ViewStateSnapshot.DisplayStyle` 필드는 타입을 `Autodesk.Revit.DB.DisplayStyle`로 완전한 이름을 써야 했다, 안 그러면 CS0246), `View3D` 전용(`SectionBoxActive`/`SectionBox`, `IsPerspective`/`Orientation`(모드 자체가 다르면 `ToggleToPerspective()`/`ToggleToIsometric()` 호출 후 `SetOrientation`으로 카메라 복원), `RenderingSettings`).
    - **최선 노력 캐치올** (`GraphicsIntegerParams: Dictionary<string, int>`): 그림자/태양경로/스케치라인 등 "그래픽 화면표시 옵션" 대화상자의 개별 항목은 Revit 공개 API에 전용 getter/setter가 없다 — 대신 `view.Parameters`를 순회해 `Definition.GetGroupTypeId() == GroupTypeId.Graphics`(구 `BuiltInParameterGroup.PG_GRAPHICS`—**CONFIRMED**: 이 프로젝트가 지원하는 Revit 버전대의 API에서 `BuiltInParameterGroup`/`Definition.ParameterGroup` 자체가 없어 CS0103/CS1061로 컴파일이 깨졌다, `GroupTypeId`(ForgeTypeId 기반) API로 교체해서 5개 연도 전부 빌드 확인함)이고 `StorageType.Integer`이고 `IsReadOnly==false`인 파라미터를 이름(`Definition.Name`) 기준으로 전부 캡처/복원한다. 이 값들이 실제로 그림자/스케치라인 등을 포함하는지는 **라이브 테스트로만 확인 가능** — 안 되는 항목이 있으면 이 캐치올이 그 파라미터를 못 잡고 있다는 뜻이니 이 지점부터 확인할 것.
  - **툴바 위치 → PC 전역 저장** (2026-07-28, "빠른토글 창의 위치는 로컬에 저장되어 어떤 프로젝트를 열더라도 그 위치는 그대로있어야한다"): `ToolbarOffsetXDip`/`ToolbarOffsetYDip`을 프로젝트별 `QuickToggleSettings`에서 떼어내 새 `QuickToggleGlobalSettings`(`%APPDATA%\WallSplitter\quick-toggle\toolbar-position.json`, 프로젝트 경로와 무관)로 옮겼다. `QuickToggleToolbar`는 생성자에서 한 번만 로드해 `_globalSettings`에 캐시하고, 드래그 시(`DragGrip_MouseLeftButtonDown`)·위치 계산 시(`RepositionToMainWindow`) 전부 이걸 참조한다. `QuickToggleSettingsWindow`의 "위치 초기화" 버튼은 이제 이 창의 "저장"을 거치지 않고 즉시 리셋·저장하며(전역 설정이라 프로젝트별 저장 흐름과 분리됨), 열려 있는 툴바에는 `QuickToggleToolbar.Instance.ReloadGlobalSettings()`로 바로 반영한다.
  - **설정 내보내기/가져오기 (다른 모델 간 이식)** (2026-07-28, "이 설정은 다른 모델에서 설정된 빠른토글설정을 불러올 수 있으며, 불러오기를 하는 모델에 기존에 있던 설정은 그대로 두고 다른모델에서 불러온 설정을 덮어씌우는 방식으로 불러올 수 있다 ... 없는 요소들도 함께 불러올 것인지 말 것인지를 사용자가 직접 선택"): `QuickToggleButtonConfig`에 `ViewTemplateName`/`FilterNames`/`WorksetNames`(이름 기반 필드)를 추가 — `ElementId`는 문서마다 달라 이식 불가능하므로, `QuickToggleSettingsWindow`의 라디오/체크박스 핸들러가 ID를 설정하는 지점마다 이름도 같이 채운다. 가져온 버튼은 새 `Guid`를 발급해 **기존 `_settings.Buttons`에 추가**할 뿐 지우지 않으며(요청사항), 창의 "저장"을 눌러야 파일에 최종 반영된다(다른 편집과 동일). **타입 충돌**: 이 파일에 `Autodesk.Revit.UI`를 통째로 `using`하면 그 네임스페이스의 `TextBox`가 WPF `TextBox`(이 파일이 이미 여러 곳에서 씀)와 CS0104로 충돌한다 — `TaskDialog`/`TaskDialogCommonButtons`/`TaskDialogResult` 세 타입만 `using X = Autodesk.Revit.UI.X;` 별칭으로 가져와 해결했다(`Grid`를 `WpfGrid`로 별칭 처리한 기존 관례와 같은 종류의 충돌).
    - **대상 자체를 함께 이식 + 모델 간 직접 전송** (2026-07-30, 재요청 — "설정해둔 버튼에 해당하는 대상(예를 들면 xxx필터, 작업세트)을 함께 내보내고 가져오기가 되었으면 좋겠고 같은 이름을 사용하는 필터나 뷰템플릿,작업세트 등이 있을때에는 덮어 씌울건지 물어봐줘. 그리고 내보내기를 json파일로 내보내기, 모델로 내보내기 두가지로 할 수 있게 해줘, 모델로 내보내기를 하면 동일한 revit창에 띄워져있는 다른 프로젝트로 내보내기 또는 가져오기가 가능하도록"): **JSON 파일에는 이름 문자열만 담을 수 있고 실제 Revit 요소는 담을 수 없다** — 그래서 "대상을 실제로 함께 가져오기"는 JSON 경로가 아니라, 같은 Revit 세션에 열려 있는 다른 프로젝트 문서와 살아있는 `Document` 참조를 직접 주고받는 새 경로("모델로 내보내기"/"모델에서 가져오기")에서만 가능하다. 버튼 4개로 나눴다: `ExportJsonButton`(기존 JSON 내보내기), `ImportJsonButton`(기존 JSON 가져오기, 이름 매칭만), `ExportToModelButton`/`ImportFromModelButton`(신규).
      - **문서 선택** (`OpenDocumentPickerWindow.xaml(.cs)`, 신규): `Document.Application.Documents`를 순회해 링크 문서(`IsLinked`)·패밀리 문서(`IsFamilyDocument`)·현재 문서 자신을 제외한 목록을 라디오 버튼으로 보여준다(`QuickToggleSettingsWindow.OpenOtherDocuments`).
      - **실제 복사** (`QuickToggleTransferService.CopyNamedElement`): `ElementTransformUtils.CopyElements`로 소스 문서의 뷰템플릿/필터를 대상 문서로 복사한다.
        - **CONFIRMED 코드 결함, 수정 (2026-07-30, 사용자가 "뷰템플릿과 필터도 함께 이동이 되도록 모델에서 모델로 복사가 안되는거야?"라고 재차 물어봐서 웹 검색으로 원인 발견)**: 처음 구현할 때 `CopyPasteOptions`를 기본값(`new CopyPasteOptions()`)으로만 넘겼다. 뷰템플릿/필터는 자기 자신 말고도 선패턴·채우기 패턴·재료 같은 하위 "타입" 요소를 함께 참조하는데, 그 타입 이름이 대상 문서에 이미 있으면 Revit은 "같은 이름의 타입이 있다 - 기존 걸 쓸지 새로 복제할지" 판단이 필요한 상황으로 본다. 이 판단 로직(`IDuplicateTypeNamesHandler`)을 지정하지 않으면 원래 사용자가 보는 대화상자로 물어보게 되어 있는데, 이 창처럼 자동으로 실행되는 문맥에서는 그 대화상자 자체가 뜨지 않거나 `CopyElements` 호출이 조용히 아무것도 복사하지 않고 넘어가는 원인이 됐을 것으로 보인다(Jeremy Tammik/thebuildingcoder, Autodesk 공식 API 문서, Autodesk Community 포럼 검색으로 확인 - 뷰(뷰템플릿 포함)는 문서 간 `CopyElements`로 복사 가능하다는 것 자체는 SDK의 `DuplicateViews` 샘플로 확인됐고, 그 샘플이 정확히 이 중복 타입 이름 처리를 위해 커스텀 핸들러를 쓰고 있었다). 고정: `AutoUseDestinationTypesHandler`(`IDuplicateTypeNamesHandler` 구현, 항상 `DuplicateTypeAction.UseDestinationTypes` 반환 - "이름이 같은 하위 타입은 항상 대상 문서에 있는 걸 그대로 쓴다")를 `CopyPasteOptions.SetDuplicateTypeNamesHandler`로 지정했다. 곁들여, 복사 중 뜰 수 있는 경고 대화상자도 자동 실행 문맥에서 안 보이거나 멈춘 것처럼 보일 위험이 있어 `SilentWarningsPreprocessor`(`IFailuresPreprocessor` 구현, `FailureSeverity.Warning`만 조용히 `DeleteWarning` — 오류는 그대로 둬서 트랜잭션이 정상적으로 실패하게 함)를 트랜잭션의 `FailureHandlingOptions`에 지정했다(`QuickToggleSettingsWindow.TransferButtons`).
        - **여전히 라이브 테스트 필요**: 이 수정으로 문제가 해결됐는지는 이 개발 환경에 실행 중인 Revit이 없어 직접 확인하지 못했다 — 웹 검색으로 찾은 원인 분석과 API 계약(모두 5개 연도에서 컴파일 확인)에 근거한 수정이지, 라이브 재현 후 고친 것은 아니다. 여전히 복사가 안 되면 이 지점(특히 `CopyPasteOptions`/`IDuplicateTypeNamesHandler`)부터 다시 확인할 것.
      - **덮어쓰기 확인**: 가져올 이름이 대상 문서에도 있고(이미 존재) 소스 문서에도 있으면(진짜로 복사할 원본이 있음) `TaskDialog`(예/아니오)로 한 번에 물어본다 — "예"=대상의 기존 요소를 지우고(`Document.Delete`) 소스 것으로 새로 복사, "아니오"=대상에 있는 걸 그대로 사용. JSON 가져오기는 애초에 소스 요소가 없어 이 질문 자체가 뜨지 않는다(항상 "아니오"와 동일하게 동작 - 기존 걸 이름으로 찾을 뿐). **덮어쓰기의 위험**: 기존 요소를 삭제하면 그 요소를 참조하던 다른(이번 가져오기와 무관한) 커스텀 버튼이 끊어진 참조를 갖게 될 수 있다 - `QuickToggleService`의 기존 방어(삭제된 대상은 조용히 건너뜀)가 크래시는 막아주지만, 그 버튼은 다시 대상을 지정해야 정상 동작한다. 작업세트는 "내용"이 없어(이름뿐) 덮어쓸 게 없다 - 대상에 없으면 그냥 새로 만든다(`QuickToggleTransferService.EnsureWorkset`, `Workset.Create`).
      - **트랜잭션**: 뷰템플릿/필터 복사와 작업세트 생성 전부를 `QuickToggleSettingsWindow.TransferButtons` 안에서 트랜잭션 하나로 묶는다(대상 문서 기준) — JSON 가져오기(소스 문서 없음)는 대상 문서를 전혀 건드리지 않으므로 트랜잭션 자체를 열지 않는다.
      - **"모델로 내보내기"의 저장 시점**: 대상이 이 창이 열려 있는 문서(`_doc`)가 아니라 "다른" 열린 문서이므로, 이 창의 "저장" 버튼 흐름을 타지 않는다 — 대상 문서의 `QuickToggleSettings`를 즉시 불러와(`QuickToggleSettings.Load`) 병합하고 그 자리에서 바로 그 문서 파일에 저장한다(`targetSettings.Save(targetDoc)`). 내보낼 버튼 목록은 `_settings.Buttons`를 JSON 왕복 직렬화로 깊은 복제해서 넘긴다 - `TransferButtons`가 `ViewTemplateId` 등을 대상 문서 기준으로 덮어써버리므로, 원본(이 창이 계속 쓰고 있는 `_settings.Buttons`)이 오염되면 안 되기 때문.
      - 카테고리(프리셋/색상 버튼)는 이 경로에서도 복사 대상이 아니다 - Revit 카테고리는 고정된 분류 체계라 여전히 이름(+상위 카테고리 이름)으로만 다시 찾는다.
- **프리셋 버튼 (뷰템플릿+필터+작업세트 조합을 한 버튼으로)** (2026-07-28, 사용자 요청 — "그 여러가지 설정의 조합으로 하나의 프리셋버튼으로 만들 수 있는 커스텀 버튼 설정에서의 기능이 중요할 것 같아", "구조 협의 모드"처럼 여러 토글을 한 번에 켜고 끄면 체감 효용이 크다는 논의 끝에 결정): `QuickToggleCategory`에 `Preset`을 추가했다. 데이터 모델은 새로 만들 필요가 없었다 — 기존 `QuickToggleButtonConfig`가 이미 `ViewTemplateId`/`FilterIds`/`WorksetIds`를 전부 필드로 갖고 있었고(단일 카테고리 버튼은 그중 하나만 채워 쓰던 것), 프리셋은 이 셋을 동시에 채우기만 하면 된다.
  - **핵심 설계 결정 — "비어있음 = 이 프리셋에 그 항목이 없음"**: 단일 카테고리 버튼에서 `ViewTemplateId == null`은 "off일 때 뷰템플릿을 지운다"는 뜻이지만, 프리셋에서는 의미가 다르다 — "이 프리셋은 애초에 뷰템플릿을 다루지 않는다, 건드리지 마라"는 뜻이어야 한다. 그래서 `QuickToggleService.TogglePreset`/`DeterminePresetState`(신규, `Toggle`/`DetermineState`의 `Preset` case에서 호출)는 `ViewTemplateId.HasValue`/`FilterIds.Count>0`/`WorksetIds.Count>0`로 "이 부분이 프리셋에 포함되는지"부터 확인하고, 포함된 부분만 판정·적용한다 — 특히 뷰템플릿은 기존 `ViewTemplate` case를 그대로 재사용하면 `cfg.ViewTemplateId`가 없을 때 매번 `ElementId.InvalidElementId`로 강제 초기화해버려서(단일 카테고리 버튼엔 맞는 동작이지만) 프리셋에 뷰템플릿이 빠져 있을 뿐인데 클릭할 때마다 현재 뷰의 뷰템플릿을 지워버리는 회귀가 생겼을 것 — 그래서 별도 메서드로 분리했다(기존 세 case는 그대로 두고 손대지 않음, 이 코드베이스의 "작은 로직은 공유보다 복제" 관례와 같은 이유). 하나도 포함된 부분이 없으면(버튼을 만들고 아직 아무것도 안 고른 상태) `Disabled`. 워크셰어링 안 된 문서에서 작업세트가 포함된 프리셋은 그 부분만 조용히 판정/적용에서 제외한다(문서 전체를 Disabled로 만들지 않음 — 다른 부분(뷰템플릿/필터)은 여전히 동작해야 하므로).
  - **설정 UI**: "+ 프리셋 버튼 추가" 버튼(`QuickToggleSettingsWindow.xaml`)으로 추가하면, 편집 영역에 뷰템플릿 라디오(`<없음>` 포함) + 필터 체크박스 + 작업세트 체크박스 세 섹션이 각자 "비워두면 이 프리셋은 이 항목을 건드리지 않습니다" 안내와 함께 순서대로 나온다(`BuildEditPanel`이 `Category==Preset`일 때 세 `Build*Picker`를 헤더와 함께 연달아 호출 — 단일 카테고리 때의 고정 헤더(`EditHeaderHost`)는 안 쓰고 스크롤 영역(`EditPanelHost`) 안에 세 헤더를 같이 넣는다, 3섹션이라 고정 영역에 넣기엔 너무 길어서).
  - **내보내기/가져오기**: 이름 기반 재검색 로직(`ImportButton_Click`)이 원래 `switch(cfg.Category)`로 분기했던 걸, 카테고리 구분 없이 세 필드(뷰템플릿 이름 하나 + 필터 이름 리스트 + 작업세트 이름 리스트)를 항상 다 시도하도록 통합했다 — 단일 카테고리 버튼은 어차피 나머지 두 필드가 비어있는 채로 들어오니 그 부분은 그냥 아무 일도 안 하고, 프리셋은 세 부분이 다 채워져 있어도 하나의 로직으로 처리된다.
  - **아이콘**: 기본 아이콘은 `Star`(`QuickToggleIcons.DefaultFor`) — 기존 6종 카테고리 기본값(레이어/깔때기/리스트)과 구분되는 "여러 개를 묶은 대표 설정"이라는 인상을 주기 위해 골랐다. 사용자가 아이콘/색을 직접 바꾸는 기존 메커니즘 그대로 적용된다.
  - **탭 UI + 카테고리(V/G) 탭 추가** (2026-07-29, 사용자 요청 — "스크롤로 하나씩 찾아서 고르는 식이 아니라 카테고리 탭형식으로 클릭해서 들어가서 선택할 수 있어야해. 그리고 V/G 편집창에 있는 것들을 그대로 다 옮겨 넣어서 탭에 넣어 그걸로 프리셋버튼을 만들 수 있게 해줘야해" — 이어서 "V/G 탭은 어느 수준까지?"/"카테고리 목록 구성은?"을 확인해 "완전한 V/G 그래픽 재정의까지" + "모델/주석/해석모델/가져온 카테고리/필터/작업세트 이렇게 보여주자"로 확정):
    - 기존에 세 섹션(뷰템플릿/필터/작업세트)을 `EditPanelHost` 하나에 이어붙여 스크롤하던 방식을, `TabControl`(`Theme.xaml`에 이미 스타일이 있어 그대로 재사용 — 이 코드베이스 최초의 `TabControl` 사용례) 기반 7개 탭(뷰템플릿/필터/작업세트/모델 카테고리/주석 카테고리/해석 모델 카테고리/가져온 카테고리)으로 바꿨다(`QuickToggleSettingsWindow.BuildPresetTabs`). 각 체크박스/편집 동작마다 `BuildEditPanel`을 통째로 다시 그리므로, 마지막으로 보던 탭을 `_lastPresetTabIndex`(세션 한정 필드)에 기억해뒀다가 복원한다 — 안 그러면 카테고리 탭에서 체크할 때마다 첫 탭("뷰템플릿")으로 튕겨 보인다.
    - **카테고리 그룹 분리**: 모델/주석/해석모델은 `doc.Settings.Categories`를 `Category.CategoryType`으로 걸러 쓴다(`QuickToggleService.TopLevelCategoriesOfType`). "가져온 카테고리"(CAD 가져오기 레이어)는 이 트리에 없어서(가져오기별로 동적 생성) 별도로 `Category.GetCategory(doc, BuiltInCategory.OST_ImportObjectStyles)`의 하위 카테고리에서 모은다(`QuickToggleService.ImportedCategories`, 이 루트 카테고리가 없는 문서 — 가져온 CAD가 아예 없는 경우 — 를 방어적으로 처리). 각 최상위 카테고리는 하위 카테고리가 있으면 펼침/접힘 토글(`_expandedCategoryIds`, 이 창 한정 세션 상태)로 트리처럼 보여준다.
    - **데이터 모델**: `QuickToggleButtonConfig.CategoryOverrides`(`List<CategoryOverrideConfig>`) — 카테고리가 이 리스트에 있다는 것 자체가 "이 프리셋에 포함됨"(Preset의 다른 필드들과 같은 "비어있으면 안 건드림" 규칙). `CategoryOverrideConfig`는 Revit V/G 대화상자에서 카테고리 한 줄이 가질 수 있는 항목(표시/하프톤/상세수준/투명도 + 투영선·절단선의 두께·색상·패턴 + 표면·절단 패턴의 전경/배경 표시·패턴·색상)을 전부 nullable 필드로 담는다 — null=재정의 안 함, 값 있음=그 값으로 재정의. 색상은 `int`(0xRRGGBB), 선/채우기 패턴은 **이름**으로 저장(`ElementId`는 문서마다 달라 이식 불가 — `ViewTemplateId`/`Name` 쌍과 같은 이유). 카테고리 자신도 `CategoryId`+`CategoryName`+`ParentCategoryName`(최상위면 null)을 함께 저장 — 같은 이름의 하위 카테고리가 서로 다른 상위에 있을 수 있어 가져오기 때 매칭 정확도를 위해 부모 이름까지 둔다.
    - **적용/판정** (`QuickToggleService.ApplyCategoryOverrides`/`BuildOverrideGraphicSettings`, `TogglePreset`/`DeterminePresetState`에 통합): 켜질 때 `Visible`이 있으면 `SetCategoryHidden`, 나머지 재정의는 매번 새 `OverrideGraphicSettings`를 만들어(반영된 값만 `Set*` 호출) `SetCategoryOverrides`로 적용. 꺼질 때는 표시로 되돌리고 빈 `OverrideGraphicSettings`로 교체(재정의 완전히 지움) — "되돌리기" 스냅샷 기능과 달리 프리셋은 켜짐/꺼짐 두 상태를 직접 정의하는 필터/작업세트와 같은 사고방식이라, 끄기가 "원래 뭐였는지"가 아니라 "표시+재정의 없음"이라는 고정 상태다. on/off 판정(`DeterminePresetState`)은 `Visible`을 지정한 카테고리에서만 표시 여부를 비교한다 — 선/패턴/투명도는 `OverrideGraphicSettings`에 동등성 비교가 없어 정확히 비교할 방법이 마땅치 않기 때문. **CONFIRMED 코드 결함, 커밋 전 리뷰로 발견·수정**: 처음엔 `anyPart`를 `Visible`이 있는 카테고리에서만 켰는데, 그러면 "표시 여부는 안 건드리고 색상/패턴만 재정의하는" 프리셋은 `anyPart`가 끝까지 `false`로 남아 `Disabled`를 반환했고, 툴바는 `Disabled` 버튼을 `IsEnabled=false`로 그려서 그런 프리셋이 클릭 자체가 막혔을 것 — `CategoryOverrides.Count > 0`이면 무조건 `anyPart=true`로 고쳤다(on/off 판정 자체는 그대로 `Visible` 있는 항목만 본다).
    - **패턴 이름 재검색은 문서 무관, 카테고리만 이식 시 재매칭**: 선/채우기 패턴 이름은 가져오기 시점에 미리 확인하지 않는다 — `BuildOverrideGraphicSettings`가 Toggle을 실제로 실행하는 매 순간 그 문서에서 이름으로 다시 찾고(`ResolveLinePatternId`/`ResolveFillPatternId`), 없으면 그 속성만 조용히 건너뛴다(카테고리 자체가 없어지는 것과 달리 "이 패턴만 빠짐"이라 프리셋 전체를 막을 이유가 없음). "실선"/"실채우기"는 실제 `LinePatternElement`/`FillPatternElement`가 아니라 Revit이 특수 취급하는 값이라 예약 문자열(`QuickToggleService.SolidLinePatternName`/`SolidFillPatternName`)로 감지한다(`LinePatternElement.GetSolidPatternId()`, `FillPatternElement.GetFillPattern().IsSolidFill`로 문서 로캘과 무관하게 찾음). 가져오기(`ImportButton_Click`)는 `CategoryOverrides`의 `CategoryId`만 (`CategoryName`,`ParentCategoryName`) 키로 재매칭하고, 없으면 그 카테고리 항목 자체를 누락 목록에 추가하고 버린다.
    - **편집 창** (`CategoryOverrideEditWindow.xaml(.cs)`, 신규): 카테고리 한 줄의 "편집" 버튼으로 열리는 모달 — Revit이 실제로는 선 재정의/패턴 재정의를 별도 하위 대화상자로 나누지만, 카테고리마다 여러 창을 열게 하는 대신 스크롤 하나에 전부 담았다(이 코드베이스가 커스텀 컨트롤을 최소화하는 관례와도 맞음). 3상태(`IsThreeState`) 체크박스의 Indeterminate를 `bool?`의 `null`(재정의 안 함)에 그대로 대응시켜 표시/하프톤/패턴 전경·배경 표시 여부를 표현하고, 두께/패턴/상세수준은 콤보박스 맨 앞에 "재정의 안 함" 항목을 두는 방식, 투명도는 별도 "재정의" 체크박스로 슬라이더를 활성/비활성한다. Cancel 시 원본이 안 바뀌도록 `CategoryOverrideConfig.Clone()`으로 복제본을 편집하고 OK를 눌러야만 `cfg.CategoryOverrides`의 항목을 교체한다.
    - **색상 선택** (`ColorPickerPopupWindow.xaml(.cs)`, 신규): 이 코드베이스가 전체 색상환 같은 커스텀 컨트롤을 안 쓰는 관례라, 자주 쓰는 색 16종 팔레트(클릭 즉시 적용) + 16진수 직접 입력(정확한 색이 필요할 때)으로 구성 — `WindowsForms.ColorDialog` 등 새 의존성을 추가하지 않았다.
    - **네임스페이스 충돌**: `CategoryOverrideEditWindow.xaml.cs`도 `Autodesk.Revit.DB`와 `System.Windows.Media`를 같이 쓰다 보니 `Color`가 모호해져(기존 `QuickToggleSettingsWindow`의 `Grid`/`Panel` 충돌과 같은 종류) `System.Windows.Media.Color`로 완전한 이름을 썼다. `QuickToggleSettingsWindow.xaml.cs`에서도 이번에 처음으로 `Panel`(피커들이 대상 컨테이너를 매개변수로 받도록 리팩터링하며 추가) 충돌이 발생해 `System.Windows.Controls.Panel`로 완전한 이름을 썼다 — `Autodesk.Revit.DB`에도 커튼월 패널을 나타내는 `Panel` 클래스가 있다.
- **색상 버튼 (모델 카테고리 색상/투명도 실시간 조절)** (2026-07-29, 사용자 요청 — "모델을 선택해서 색상과 투명도를 설정해줄 수 있는 버튼을 추가하고 싶어. 설정창에서 색상버튼과 모델카테고리에서 어떤 카테고리에 색상을 적용할 것인지 선택을 하고, 저장한다음, 커스텀 버튼창에서 만든 색상버튼을 클릭하면 창의 위치에따라 상단/하단으로 UI가 펼쳐지면서 활성화되어있는 뷰의 모델 카테고리에 색상을 지정해줄 수 있고, 투명도를 조절해줄 수 있는 버튼이 있으면 좋겠어"): 새 카테고리 `QuickToggleCategory.ColorTool` — 지금까지의 버튼(뷰템플릿/필터/작업세트/프리셋)은 전부 "켜짐/꺼짐"을 토글하는 방식이었지만, 이건 근본적으로 다르다 — 클릭하면 색상 팔레트 + 투명도 슬라이더가 담긴 작은 패널이 뜨고, 그 안에서 조작할 때마다 활성 뷰에 실시간으로 반영된다.
  - **데이터 모델**: `QuickToggleButtonConfig.ColorButtonCategories`(`List<CategoryOverrideConfig>`) — 이 버튼이 색상/투명도를 적용할 모델 카테고리 목록. `CategoryOverrideConfig`를 재사용했지만 `CategoryId`/`CategoryName`/`ParentCategoryName`만 쓰고 나머지 재정의 필드(선/패턴 등)는 이 용도에서 전혀 안 쓴다 — 새 타입을 만드는 대신 기존 타입과 그 이름 기반 가져오기 매칭 로직을 그대로 재사용하기 위함. **실제 색상/투명도 값 자체는 설정에 저장하지 않는다** — 클릭할 때마다 그 카테고리의 현재 값을 Revit에서 읽어와 보여준다(`QuickToggleService.ReadCurrentColorAndTransparency`).
  - **설정 UI**: "+ 색상 버튼 추가" 버튼으로 만들면, 편집 화면에 모델 카테고리 트리(체크박스만, 프리셋의 `BuildCategoryRow`와 달리 "편집" 버튼도 재정의 요약도 없음 — 실제 값은 여기서 정하지 않으므로)가 뜬다(`QuickToggleSettingsWindow.BuildColorToolCategoryRow`, `BuildCategoryRow`와 거의 같은 모양이지만 이 코드베이스의 "작은 UI는 공유 대신 복제" 관례에 따라 별도 메서드로 둠).
  - **적용 로직** (`QuickToggleService.ApplyColorTool`): 프리셋의 카테고리 재정의(`ApplyCategoryOverrides`, 켜짐/꺼짐 두 고정 상태를 매번 새로 만듦)와 달리, 이건 "지금 뷰에 이미 적용된 재정의를 먼저 읽고 그 위에 이번에 바뀐 속성만 얹는" 방식이다 — 색상만 바꿨을 때 이미 슬라이더로 조절해둔 투명도가 지워지면 안 되고, 그 반대도 마찬가지이기 때문. 색상은 "실채우기(Solid fill) 패턴 + 그 색"을 투영면·절단면 양쪽에 적용하는 방식으로 지정한다(패턴만 바꾸고 색을 안 주면 흰 바탕에 무늬만 남으므로) — Revit에서 실제로 "카테고리를 이 색으로 칠한다"고 할 때 쓰는 방식과 동일. 투명도는 `SetSurfaceTransparency`.
  - **초기값 읽기** (`ReadCurrentColorAndTransparency`): 패널을 열 때 대상 카테고리 중 첫 번째의 현재 `OverrideGraphicSettings`를 읽어 팔레트/슬라이더를 "이번에 새로 고르는 것"이 아니라 "지금 적용된 값"에서 시작하게 한다. 색상은 `Color.IsValid`가 거짓이면(한 번도 지정한 적 없음) null로 취급하고, 투명도는 Revit이 "재정의된 적 있는지"를 별도로 노출하지 않아 손댄 적 없는 카테고리도 항상 0으로 읽힌다 — Revit V/G 대화상자 자체도 같은 방식으로 보여주므로 그대로 따랐다.
  - **팝업 창** (`ColorToolPopupWindow.xaml(.cs)`, 신규): 모달이 아닌(`Show()`) 창 — 열려 있는 동안 계속 값을 바꿔가며 실시간 미리보기하는 용도라 "확인" 버튼이 없고, 팔레트 클릭/16진수 입력/슬라이더 조작 각각이 그 즉시 `ExternalEvent`로 반영된다(다른 버튼들과 같은 이유 — 모드리스 창 클릭 시점엔 유효한 API 컨텍스트가 없음). **어느 뷰에 적용할지는 팝업을 열 때 캡처해두지 않는다** — `QuickToggleExternalEventHandler.ExecuteColorApply`가 실행되는 바로 그 순간의 활성 뷰를 다시 조회한다(팝업이 열려있는 동안 사용자가 다른 뷰로 전환했을 수 있으므로).
    - **펼침 방향** (`QuickToggleToolbar.ShowColorToolPopup`): 클릭한 버튼의 화면 좌표(`Button.PointToScreen`)와 `SystemParameters.WorkArea.Bottom`을 비교해, 버튼 아래로 펼쳤을 때 화면 밖으로 나갈 정도로 화면 하단에 가까우면 위로, 아니면 아래로 펼친다. 패널은 `SizeToContent="Height"`라 `Show()` 전엔 정확한 높이를 모르므로, 위로 펼치는 경우엔 어림값으로 먼저 배치한 뒤 `Loaded` 이벤트에서 실제 `ActualHeight`로 위치를 보정한다. **멀티 모니터 미검증**: `SystemParameters.WorkArea`는 주 모니터 기준이라, 툴바가 보조 모니터에 떠 있으면 펼침 방향 판정이 부정확할 수 있다 — `System.Windows.Forms.Screen` 같은 새 의존성을 추가하지 않기로 한 이 프로젝트의 기존 선택(`ColorPickerPopupWindow`와 동일)과 같은 트레이드오프.
    - 한 번에 패널을 하나만 띄운다(`_openColorPopup`/`_openColorPopupButtonId`) — 같은 색상 버튼을 다시 누르면 닫고(토글), 다른 색상 버튼을 누르면 기존 패널을 닫고 새로 연다. 툴바가 숨겨지거나(`Hide()`/`HideForNoDocument`) 문서가 아예 없어지는 경우에도 열린 패널을 같이 닫아 고아 창이 남지 않게 한다.
    - **"재지정 지우기" 버튼** (2026-07-29 추가, "색상버튼에서 선택한 카테고리 요소에 입혀진 색상이 아무것도 없게 만들어주는 '재지정 지우기' 버튼도 추가해줘"): `ApplyColorTool`(기존 재정의를 읽어 바뀐 속성만 얹음)과 달리 `QuickToggleService.ClearColorTool`은 대상 카테고리의 `OverrideGraphicSettings`를 빈 값으로 통째로 교체한다 — 색상/패턴/투명도/하프톤 등 이 카테고리에 걸린 재정의를 전부 지운다(프리셋 카테고리 탭을 끌 때의 방식과 동일). `ColorToolApplyRequest.Clear`(bool)로 같은 `PendingColorApply`/`ExternalEvent` 경로를 재사용한다(Clear가 true면 Color/Transparency는 무시). **지운 뒤 패널 자체를 다시 그릴 때 뷰를 다시 읽지 않는다** — `ExternalEvent.Raise()`는 비동기라 그 직후 뷰를 다시 읽으면 아직 반영 전의 값을 보여줄 위험이 있어(라이브 확인은 안 됐지만 이 아키텍처의 다른 곳(뷰 저장/되돌리기 등)과 같은 이유로 조심), `RenderControls(null, 0)`으로 "지운 상태가 어떤 모습이어야 하는지"를 이미 알고 있는 값으로 즉시 다시 그린다(팔레트 선택 해제, 16진수 칸 비움, 슬라이더 0%). 이 리팩터로 `BuildContent`(뷰를 읽어 초기 렌더링)와 `RenderControls`(실제 컨트롤 생성, 이름 레이블만 남기고 나머지를 지운 뒤 다시 그림)로 나뉘었다.
  - **`DetermineState`/`Toggle`**: `ColorTool`은 켜짐/꺼짐 개념이 없어 `DetermineState`가 항상 `Off`를 고정 반환한다 — `Disabled`를 반환하면 툴바가 `IsEnabled=false`로 그려 클릭 자체가 막히므로(다른 카테고리들의 "대상 미지정 시 Disabled" 관례를 그대로 따르면 이 버튼이 영구히 눌리지 않는 회귀가 생길 뻔했다, 프리셋의 `anyPart` 결함과 같은 종류의 함정이라 설계 단계에서 미리 피했다). 클릭도 `Toggle()`/`ExternalEvent`의 on/off 경로를 아예 타지 않는다 — `QuickToggleToolbar.ToggleButton_Click`이 `Category==ColorTool`이면 그 자리에서 곧장 `ShowColorToolPopup`으로 분기하고, 패널 내부 조작만 별도의 `ColorToolApplyRequest`/`PendingColorApply`로 같은 `ExternalEvent`를 재사용한다.
  - **툴바 모양 차별화** (2026-07-30, "색상버튼은 다른 버튼과는 모양이 좀 달랐으면 좋겠어. 얘는 on/off가 의미 없으니까, 작은 리본버튼으로 두줄로 들어가게 만들어줘"): `QuickToggleToolbar.RebuildButtons`가 `cfg.Category`로 분기해, 다른 버튼들의 "큰 아이콘(28x22) 위 + 라벨 아래" 세로 배치 대신 Revit 리본의 "작은" 버튼 스타일 — 작은 아이콘(16x13)이 왼쪽, 라벨이 오른쪽에 최대 두 줄까지 줄바꿈(`TextWrapping.Wrap`, `Width=46`)되는 가로 배치를 쓴다. 아이콘/라벨 재색칠(`UpdateButtonStates`)은 카테고리와 무관하게 같은 `Canvas`/`TextBlock` 참조를 그대로 쓰므로 이 배치 차이와 무관하게 계속 동작한다. `ToolTipFor`도 `ColorTool`이면 "클릭하면 켭니다/끕니다" 대신 "클릭하면 색상/투명도 조절 패널을 엽니다"로 문구를 따로 뒀다 — on/off 개념이 없는 버튼에 켜고 끈다는 문구가 붙는 게 어색해서.
    - **2줄 묶음으로 쌓기** (2026-07-30, 재요청 — "색상버튼이 작게 한줄로 들어가 있어, 버튼들이 두줄로 나열되게 만들어줘": 버튼 자체를 2줄 라벨로 줄바꿈해도 여러 개를 등록하면 그냥 한 줄로 계속 옆으로 늘어났던 문제): 색상 버튼들을 `ButtonsPanel`에 직접 넣지 않고, 처음 만나는 색상 버튼에서 `Orientation=Vertical`, `Height=64`(`ColorToolGroupHeightDip`, 대략 색상 버튼 2개 높이)로 고정한 `WrapPanel`을 하나 만들어 그 안에 전부 모아 넣는다 — `WrapPanel`이 세로 방향으로 이 높이만큼 채우고 나면 자동으로 다음 칸(열)으로 넘어가므로, Revit 리본에서 "작은" 버튼들이 패널 안에 2단으로 쌓이는 것과 같은 모양이 된다. 일반 버튼들 사이에 색상 버튼이 여러 개 흩어져 있어도 첫 번째로 만난 위치에 그룹 하나로 모으고 나머지는 그 그룹에 추가한다(목록 순서를 완벽히 보존하기보단, Revit도 큰/작은 버튼을 줄 단위로 섞지 않는다는 점과 같은 타협). `WrapPanel`은 자식을 균등하게 늘리지 않고 각자 고유 크기 그대로 배치하므로 `Height`만 명시하면 되고(늘어남 방지), 그룹 앞에 이미 다른 버튼이 있으면 구분선(`Border Width=1`)을 하나 넣어 "여기부터 다른 종류"라는 걸 시각적으로도 표시한다.
  - **대상 검색 입력칸** (2026-07-30, "커스텀 버튼 설정창에 대상을 선택할때 검색할 수 있는 입력칸을 작게 하나 만들어줘"): 뷰템플릿/필터/작업세트 목록과 프리셋·색상 버튼의 카테고리 트리 전부에 좁은 검색 입력칸(`QuickToggleSettingsWindow.BuildSearchRow`, `Width=140`)이 붙었다. **목록만 다시 그리고 검색 입력칸 자체는 다시 만들지 않는다** — `TextChanged`마다 `BuildEditPanel` 전체를 다시 그리면 입력칸 자신도 새 인스턴스로 교체되어 한 글자 입력할 때마다 포커스가 끊긴다(이 창의 다른 곳, `QuickToggleToolbar`의 "매 틱 재대입하면 클릭이 씹힌다" 버그와 근본적으로 같은 함정이라 처음부터 피했다) — 그래서 검색 입력칸을 담은 바깥 패널은 그대로 두고, 결과 목록만 담는 별도의 `resultsPanel`을 따로 둬서 그 패널의 `Children`만 지웠다 다시 채운다(`RenderViewTemplateList`/`RenderFilterList`/`RenderWorksetList`/`RenderCategoryList`). 뷰템플릿/필터/작업세트는 이름에 검색어가 포함되는지(`MatchesSearch`, 대소문자 무시)로 단순 필터링하고, `<없음>` 라디오는 검색어와 무관하게 항상 보여준다(이름으로 찾을 대상이 아니라 "선택 해제" 기능이라서). 카테고리 트리(프리셋의 카테고리 탭, 색상 버튼의 카테고리 선택)는 검색어가 있으면 트리 구조(펼침/접힘)를 잠시 접어두고 깊이와 무관하게 이름이 일치하는 카테고리를 전부 평평하게 나열한다 — 몇 단계 아래 하위 카테고리를 찾으려고 매번 상위를 펼쳐야 하는 불편을 없애기 위함(검색어를 지우면 원래 트리로 돌아옴).
- **기능 버튼 (다른 Sunny Tools/Revit 기본 기능을 버튼으로)** (2026-08-03, 사용자 요청 — "커스텀 버튼 설정에 다른 툴들의 버튼도 추가할 수 있는 기능이 있으면 좋겠어. 예를들어 Sunny Tools의 재료지정 버튼을 추가한다던지, 네이머를 추가한다던지, 공동작업탭에 동기화버튼을 추가하는 등 내가 원하는 기능을 찾아서 버튼으로 추가하고 싶어" — 이어서 "범위는 Sunny Tools + Revit 전체 명령(PostableCommand) 검색", "클릭 즉시 실행(확인창 없음)"으로 Q&A 확정): 새 카테고리 `QuickToggleCategory.CommandLauncher` — `ColorTool`과 마찬가지로 on/off 개념이 없고(`DetermineState`가 항상 `Off` 고정 반환, 이유도 ColorTool과 동일 - `Disabled`를 반환하면 툴바가 버튼을 `IsEnabled=false`로 그려 클릭 자체가 막힌다), 클릭하면 지정된 Revit 명령을 그 자리에서 한 번 실행할 뿐이다.
  - **명령 조회/실행** (`QuickToggleService.RunCommand`): Revit API가 "임의의 명령을 프로그램적으로 실행"할 수 있게 해주는 유일한 공식 경로가 `RevitCommandId` + `UIApplication.PostCommand`다 - `ExternalCommandData`를 직접 만들어 `IExternalCommand.Execute`를 호출하는 방법은 없다(그 객체는 Revit이 커맨드를 실제로 실행할 때만 생성해 넘겨준다). 새 `QuickToggleCommandKind` enum으로 조회 API가 갈린다: `SunnyTool`이면 `RevitCommandId.LookupCommandId(전체 클래스 이름)`(App.cs가 `PushButtonData`를 등록할 때 쓰는 것과 똑같은 문자열), `NativeRevit`이면 `RevitCommandId.LookupPostableCommandId(PostableCommand)` - **`LookupPostableCommand`(단수)가 아니라 `LookupPostableCommandId`** - 처음 이 이름으로 짰다가 5개 연도 전부에서 CS0117(그런 멤버가 없음)로 컴파일이 깨져서 RevitAPIUI.xml을 직접 검색해 실제 멤버 이름을 확인했다. 조회에 성공하고 `uiapp.CanPostCommand(id)`가 참일 때만 `PostCommand`를 호출한다 - `PostCommand`는 즉시 실행이 아니라 "다음 기회에 실행해달라"는 요청이라(Revit 자체 리본 버튼을 누르는 것과 같은 메커니즘), 이 메서드 반환 시점엔 아직 실제로 실행되지 않았을 수 있다. 트랜잭션을 열지 않는다 - 실행될 명령 자체(재료 지정 창을 연다, 동기화한다 등)가 필요하면 각자 알아서 연다.
  - **명령 목록·한영 검색** (`SunnyToolsCommands.cs`, 2026-08-27 확장): Sunny Tools 자체 명령은 `App.cs`가 리본에 등록한 것과 같은 짧은 목록이라 검색어 없이 항상 다 보여준다. Revit 기본 명령은 `PostableCommand` enum 전체를 대상으로 하며, Autodesk 공식 Revit 2027 영문/한국어 키보드 단축키 명령표 1,840개를 `CommandId`로 합친 UTF-8 내장 리소스(`Resources/RevitCommandLabels.2027.tsv`)와 `RevitCommandId.Name`을 연결한다. 검색은 현재 화면 언어와 무관하게 영문 이름·한국어 이름·`PostableCommand` enum 이름·Revit 내부 CommandId 네 가지를 모두 검사하므로 한국어 Revit에서도 `Save`/`저장`이 모두 같은 명령을 찾는다. 결과 라벨은 `Document.Application.Language`가 `LanguageType.Korean`이면 한국어, 그 외에는 영문으로 표시한다. 설정에 저장된 `CommandLabel`은 예전 설정 및 리소스 매칭 실패의 fallback으로만 남기고, 설정 창의 "현재 선택"과 플로팅 툴바의 툴팁도 매번 현재 Revit 언어로 다시 해석한다. CommandId 매칭이나 리소스 로드가 실패한 명령은 기존 `FriendlyLabel` 영문으로 안전하게 되돌아간다. 수백 개를 필터 없이 나열하면 설정 창이 느려지고 원하는 걸 찾기도 어려우므로 검색어가 비어 있으면 Revit 기본 명령 목록은 여전히 비우고, 결과도 60개로 제한한다. **리소스를 갱신할 때는 영문/한국어 표의 CommandId 집합이 정확히 같은지와 중복이 없는지 먼저 검증해야 하며, 라벨 문자열의 탭·개행은 공백으로 정규화해야 한다.**
  - **설정 UI**: "+ 기능 버튼 추가"로 만들면 다른 라디오 목록(뷰템플릿 등)과 같은 패턴 — 검색창 + Sunny Tools 섹션(항상 표시) + Revit 기본 기능 섹션(검색어가 있을 때만) 을 같은 라디오 그룹(`"cmd_" + cfg.Id`)으로 묶어 하나만 고를 수 있다. 검색어를 바꾸면 결과 목록이 필터링되어 선택된 항목이 화면에서 사라질 수 있어(예: "NAMER"를 고른 뒤 "동기화"로 검색하면 라디오 자체가 안 보임), 검색창 위에 "현재 선택: {라벨}" 요약 줄을 항상 띄워 시각적으로 잃어버리지 않게 했다.
  - **내보내기/가져오기는 변경 없이 그대로 동작**: `CommandKind`/`CommandId`/`CommandLabel`은 `ElementId`와 달리 문서에 속한 값이 아니라 Revit/이 애드인 자체에 속한 식별자라(같은 클래스 이름, 같은 enum 멤버는 어느 문서에서나 똑같이 유효) `ViewTemplateId`/`FilterIds`처럼 별도 "이름" 필드를 두고 가져오기 시점에 다시 검색할 필요가 없다. `QuickToggleSettingsWindow.TransferButtons`(JSON/모델 이식 공용 로직)의 마지막 루프가 `ViewTemplateName`/`FilterNames`/`WorksetNames`/`CategoryOverrides`/`ColorButtonCategories`만 개별적으로 재해석하고 나머지 필드는 그대로 둔 채 `targetSettings.Buttons.Add(cfg)`하므로, `CommandLauncher` 버튼은 이 세 필드가 전부 비어있어 아무 것도 재해석되지 않고 원본 그대로 옮겨간다 - `TransferButtons`에 손댈 필요가 전혀 없었다(JSON 직렬화도 리플렉션 기반이라 새 필드가 자동으로 포함됨).
  - **여전히 라이브 테스트 필요**: `PostCommand`로 Sunny Tools 자체 명령(모달 창을 여는 것들)이 실제로 화면에 뜨는지, `SyncWithCentral`처럼 문서 상태에 따라 조건부로만 가능한 네이티브 명령이 `CanPostCommand`로 정확히 걸러지는지는 이 개발 환경에 실행 중인 Revit이 없어 확인하지 못했다 - Revit API 계약(5개 연도 전부 컴파일 확인, RevitAPIUI.xml에서 실제 멤버명 확인)에 근거한 구현이지 라이브 재현 후 고친 것은 아니다. 안 되면 이 지점(`QuickToggleService.RunCommand`)부터 확인할 것.
- **그래픽 화면표시 검색 버튼** (2026-08-27, 사용자 요청 — "커스텀 버튼바에서 바로 그래픽화면표시에서의 모델 및 주석 카테고리를 검색해서 바로 가시성, 투영 표면의 선,패턴,투명도, 잘라내기 선,패턴 중간색, 상세수준을 조절"): `QuickToggleCategory.GraphicsDisplaySearch`를 추가했다. 설정 창에서는 별도 대상 카테고리를 저장하지 않고 버튼 이름/아이콘만 정한다. 툴바에서 누르면 `GraphicsDisplaySearchPopupWindow.xaml(.cs)`가 현재 뷰에서 `GetCategoryOverrides` 또는 `GetCategoryHidden`을 읽을 수 있는 모델·주석 최상위/하위 카테고리를 모아 경로(`상위 > 하위`)와 함께 검색 결과로 보여준다. Revit 표시 언어가 카테고리 이름 자체를 현지화하므로 검색 결과도 실행 중인 Revit 언어를 그대로 따른다.
  - 결과 카테고리를 누르면 기존 `CategoryOverrideEditWindow`를 `immediateMode:true`로 재사용한다. 편집 객체는 카테고리 식별 정보 외에는 전부 null에서 시작하고, 즉시 편집 모드의 null은 "현재 재정의를 지움"이 아니라 **"이번 적용에서는 변경 안 함"**이다. 따라서 사용자는 가시성/투영선/표면 전경·배경 패턴/투명도/절단선·패턴/중간색(하프톤)/상세수준 중 바꿀 항목만 지정한다. `QuickToggleService.ApplyGraphicsDisplayOverride`는 `view.GetCategoryOverrides`로 기존 OGS를 먼저 읽고 `ApplyOverrideGraphicSettings`로 값이 있는 필드만 덧씌운 뒤 다시 저장하므로, 투명도만 바꿨을 때 기존 선·패턴 재정의가 지워지지 않는다. 가시성과 그래픽 항목을 함께 요청했다면 둘 다 성공해야 트랜잭션을 커밋하고, 한쪽이라도 실패하면 부분 적용을 남기지 않고 전체 롤백한다.
  - 검색창은 모델리스이므로 실제 Revit 변경은 `GraphicsDisplayApplyRequest`/`PendingGraphicsDisplayApply`를 통해 기존 `ExternalEvent` 처리기로 넘긴다. 요청에는 팝업을 연 문서 경로를 포함해 다른 프로젝트로 전환한 뒤 같은 정수 CategoryId에 잘못 적용되는 것을 막는다. 뷰가 바뀌면 뷰 종류별 지원 카테고리 목록도 달라질 수 있으므로 툴바의 `RefreshState`가 검색 팝업을 자동으로 닫으며, 문서 변경/툴바 숨김/설정 재로드 때도 색상 팝업과 함께 닫는다. 검색 버튼은 ColorTool과 같은 작은 도구형 버튼으로 2단 그룹에 들어가고 `DetermineState`는 항상 `Off`를 반환한다. **라이브 Revit 동작은 아직 미검증** — 2023~2027 컴파일 후 실제 뷰 템플릿 제어 항목과 카테고리별 지원 차이는 Revit에서 확인해야 한다.
- **알려진 제약**: Revit이 뷰를 별도 창으로 분리했을 때 툴바는 메인 창에만 붙고 분리된 창은 따라가지 않음; 저장 안 된 새 문서(경로 없음)는 프로젝트별 설정을 저장할 곳이 없어 툴바가 비활성 상태로 남음(최초 저장 후 정상 동작); 필터/뷰템플릿/작업세트/카테고리 대상이 삭제되면 그 항목만 조용히 무시됨; "뷰 저장" 스냅샷은 디스크에 저장되지 않으므로 Revit을 껐다 켜거나 문서를 닫았다 열면 사라짐(그 세션에서 다시 저장해야 함, 단 문서를 닫지 않고 뷰만 닫는 것은 유지됨); 저장 안 된 새 문서를 여러 개 동시에 열어두면 그 뷰 저장 스냅샷들이 전부 같은 `"__unsaved__"` 키로 뭉뚱그려짐(극히 드문 경우로 남겨둠); 그림자/태양경로/스케치라인 등 "그래픽 화면표시 옵션"의 세부 항목은 Revit API에 전용 접근자가 없어 `GraphicsIntegerParams` 캐치올로 최선 노력만 하며 실제 복원 여부는 **라이브 테스트 미검증**; 색상 버튼의 펼침 방향 판정은 주 모니터 기준이라 **멀티 모니터 환경 미검증**; 기능 버튼의 Revit 기본 명령 실행 가능 여부(`CanPostCommand`)와 실제 실행 결과는 **라이브 테스트 미검증**.
- **Revit 콜백 예외 차단 (2026-09-01, 전체 점검에서 선제 보강 — 재현된 버그는 아님)**: `App.OnQuickToggleIdling`/
  `OnQuickToggleViewActivated`/`OnQuickToggleDocumentClosing`은 `RefreshState()`를 예외 처리 없이 호출하고 있었다.
  Revit 이벤트 콜백에서 예외가 밖으로 나가면 Revit이 오류 대화상자를 띄우거나 구독 자체를 끊는데, `Idling`은
  초당 여러 번 발생하므로 한 번의 예외가 대화상자 폭주로 이어질 수 있다. 세 콜백을 `try/catch`로 감싸 조용히
  삼키고 다음 틱에 다시 갱신하게 했다(상태 표시만 하는 코드라 사용자에게 알릴 것이 없다).
  `QuickToggleExternalEventHandler.Execute`도 본문을 `ExecuteCore`로 옮기고 최상위에서 예외를 잡아 `TaskDialog`로
  알린다 — 이 파일이 이미 기록해 둔 "ExternalEvent 콜백은 실패해도 Revit이 아무 것도 보여주지 않아 '버튼을
  눌러도 반응 없음'으로만 남는다"는 문제를 예상 못 한 예외에도 똑같이 적용한 것이다.
  `QuickToggleSettingsWindow.ResetPositionButton_Click`의 `QuickToggleGlobalSettings.Save()`도 감쌌다(WPF 이벤트
  핸들러에서 예외가 새면 Revit 프로세스가 그대로 죽는다 — `SettingsWindow`의 저장 버튼과 같은 방침).
- **기능 버튼 목록에 경고Pick 누락, 수정 (2026-09-01)**: `SunnyToolsCommands.All`에 `WarningPickCommand`가 빠져
  있어(2026-08-25 경고Pick 추가 때 이 목록에 넣지 않았다) 커스텀 "기능 버튼"으로 경고Pick을 등록할 수 없었다.
  리본에 등록하는 모든 `IExternalCommand`가 이 목록에도 있어야 한다 — 새 명령을 추가하면 `App.cs`와 이 목록
  양쪽을 같이 갱신할 것.

## 프리셋·그래픽 화면표시 검색 버튼 삭제 + 링크 끄기/켜기 버튼 추가 (2026-09-02)

사용자 요청: *"커스텀 버튼 기능이 모르는 사람들이 쓰기에 너무 어려울 수도 있겠다 — 프리셋 버튼은 삭제하고
대신 링크된 도면 껏다 키기 기능을 넣어줘. 활성화되어있는 뷰를 감지해서 해당 뷰에 링크되어 있는 도면이 있으면
활성화가 되어서 클릭하면 끌 수 있는 버튼. 링크된 모델 껏다 키기도. 그래픽 화면표시 검색 버튼은 그냥 없애는 게
낫겠다 — 만든 나도 쓰기가 너무 어렵다."* 즉 **기능 축소가 목적**이므로, 삭제한 두 기능을 "숨기기"만 한 게 아니라
구현 코드·전용 창까지 전부 지웠다. 되살릴 일이 생기면 이 커밋 직전 이력에서 꺼내 쓸 것.

**지운 것** — `QuickToggleCategory.Preset`/`GraphicsDisplaySearch`의 구현 전체:
`QuickToggleService`의 `DeterminePresetState`/`TogglePreset`/`ApplyCategoryOverrides`/`BuildOverrideGraphicSettings`/
`ApplyOverrideGraphicSettings`/`ApplyGraphicsDisplayOverride`/`HasGraphicOverrideValues`/`CanEditCategoryGraphics`/
`AllLinePatternNames`/`AllFillPatternNames`/`ResolveLinePatternId`/`SolidLinePatternName`,
`QuickToggleSettingsWindow`의 프리셋 탭 UI(`BuildPresetTabs`/`BuildPresetCategoryTab`/`BuildCategoryRow`/
`SummarizeOverride`)와 추가 버튼 2개, `QuickToggleExternalEventHandler`의 `GraphicsDisplayApplyRequest`/
`PendingGraphicsDisplayApply`/`ExecuteGraphicsDisplayApply`, 창 파일 `GraphicsDisplaySearchPopupWindow.xaml(.cs)`/
`CategoryOverrideEditWindow.xaml(.cs)`/`ColorPickerPopupWindow.xaml(.cs)`(뒤의 둘은 프리셋·검색 패널에서만
쓰던 편집창이라 함께 죽은 코드가 됐다). `QuickToggleButtonConfig.CategoryOverrides` 필드도 삭제하고,
`CategoryOverrideConfig`(20여 개 V/G 재정의 필드 + `Clone()`)는 남은 유일한 사용처인 색상 버튼에 맞춰
`ColorToolCategoryConfig`(카테고리 Id/이름/부모 이름 3개 필드)로 줄였다.

- **열거 멤버 `Preset`/`GraphicsDisplaySearch`는 일부러 남겼다 (지우지 말 것)**: `QuickToggleSettings`는
  `JsonStringEnumConverter`로 카테고리를 문자열로 저장하는데, 이 컨버터는 **모르는 문자열을 만나면 예외를
  던진다**. 그리고 `QuickToggleSettings.Load`는 그 예외를 "설정 파일 손상"으로 보고 **빈 설정으로 통째로
  대체**한다 — 즉 열거 멤버를 지우면 프리셋 버튼 하나가 남아 있는 프로젝트에서 등록해 둔 다른 버튼까지 전부
  사라진다. 멤버는 남겨 역직렬화만 통과시키고, 실제 버튼은 `Load`에서 `IsRemovedCategory`로 걸러 버린다.
  같은 이유로 JSON 가져오기(`ImportJsonButton_Click`)에서도 역직렬화 직후 같은 필터를 태운다(모델 간
  가져오기는 `QuickToggleSettings.Load`를 거치므로 자동으로 걸러진다).

**새로 넣은 것** — `QuickToggleCategory.LinkedCad`(링크된 도면) / `LinkedModel`(링크된 모델):
뷰템플릿/필터/작업세트처럼 on/off 토글이지만 **설정 창에서 고를 대상이 없다**(대상은 "그때 활성 뷰에 걸려 있는
링크"라서 클릭할 때마다 새로 찾는다). 켜짐 = 링크가 보이는 상태, 링크가 하나도 없으면 `Disabled`(회색).

- **왜 `View.HideElements`가 아니라 카테고리 숨기기인가**: 링크 인스턴스를 `HideElements`로 숨기면 V/G
  대화상자에는 아무 표시도 남지 않아("숨겨진 요소 표시"를 켜야만 보인다) 이 버튼으로 끈 것을 사용자가 다른
  경로로 되돌리기 어렵다. 그래서 Revit V/G와 같은 방식(`View.SetCategoryHidden`)을 쓴다 — 링크된 CAD 도면은
  도면 파일마다 "가져온 카테고리"가 하나씩 생기므로 그 카테고리들을, 링크된 Revit 모델은 개별 링크마다
  카테고리가 생기지 않고 전부 `OST_RvtLinks` 하나에 묶이므로 그 카테고리를 끄고 켠다(그래서 링크된 모델은
  개별 링크가 아니라 항상 전부 함께 켜지고 꺼진다 — API 제약이자 "딸깍 한 번" 요청과도 맞는 동작).
- **"링크"만 대상, "가져오기"는 제외**: `ImportInstance.IsLinked`가 false인 CAD(가져오기로 넣은 것)는 목록에서
  뺀다. 뷰 전용 가져오기(`OwnerViewId`가 유효)는 그 뷰에서만 대상으로 삼고, 모델 공간에 놓인 링크는 어느
  뷰에서든 대상이다 — 평면 뷰의 뷰 범위 밖이라 실제로는 안 보이는 경우까지 가려내지는 않는다(Revit V/G의
  "가져온 카테고리" 탭도 뷰와 무관하게 문서의 링크를 전부 나열한다).
- **문서 훑기는 반드시 캐시해서 쓸 것 (`QuickToggleService.ScanLinks`, 2초)**: `DetermineState`는 툴바의
  `Idling` 갱신에서 **버튼마다 매 틱** 호출된다 — 여기서 `FilteredElementCollector`로 문서를 통째로 훑으면
  이 파일이 세 번이나 반복해서 겪은 "Idling 콜백에서 매 틱 비싼 일을 한다" 부류의 문제가 다시 생긴다.
  문서 경로별로 스캔 결과(CAD 링크의 카테고리 Id + 소유 뷰 Id, Revit 링크 유무)를 2초간 캐시한다 — 링크를
  새로 걸어도 늦어도 2초 뒤엔 버튼이 활성화된다.
- **아이콘 2종 추가(`Sheet`/`Cube`)와 `SetBrush` 변경**: 기존 도형만으로는 "도면"과 "모델"을 형태로 구분할 수
  없어 도면 틀(사각 테두리+표제란)과 아이소메트릭 정육면체를 추가했다. 둘 다 **채우지 않고 선으로만** 그리는
  첫 아이콘이라, `QuickToggleIcons.SetBrush`가 무조건 `Fill`을 대입하던 것을 "원래 칠해져 있던 쪽(`Fill` 또는
  `Stroke`)만 갱신"하도록 바꿨다 — 그대로 뒀으면 상태가 바뀔 때마다 선 그림 아이콘이 통째로 칠해진 덩어리로
  변한다.
